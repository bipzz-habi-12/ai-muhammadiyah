# Migrasi `app/page.tsx` ke Custom Hooks — Progress

## Konteks

`app/page.tsx` adalah monolith SPA chat (awalnya **4734 baris**, sekarang **3641 baris**). Migrasi ini adalah persiapan sebelum mengganti UI-nya ke desain baru dari Stitch (lihat `stitch-reference/`) — tujuannya supaya saat desain visual diganti nanti, logic/state sudah terisolasi di hooks dan tidak ikut berantakan bareng JSX.

Pendekatan: ekstrak **satu hook per langkah**, kecil dan aman, bukan big-bang refactor.

Analisis arsitektur awal (peta lengkap semua state/fungsi sebelum migrasi dimulai) ada di riwayat percakapan sesi sebelumnya — dokumen ini fokus ke **status & rencana**, bukan mengulang analisis itu.

## Status: Langkah 1–12 SELESAI (sudah di-test manual + commit) — semua ekstraksi hook domain rampung

| # | Commit | Isi |
|---|---|---|
| 1 | `a986e16` Extract pure helpers from page.tsx to lib/ and components/ | Helper murni tanpa React state dipindah ke `lib/mappers/` (types, legacy-study-mode, workspace, conversation, message), `lib/chat/` (attachments, errors, selection-labels), `lib/formatting/` (math.tsx, markdown.tsx, text.ts), `components/icons.tsx` (SparkIcon, Icon), `components/MarkdownMessage.tsx`. Dead code `formatMathLikeText`/`formatMathLikeTextV2` **dihapus** (bukan dipindah). `fetchUsageSnapshot` masuk ke `lib/usage/limits.ts`, `fetchKnowledgeSources` masuk ke `lib/knowledge.ts` (extend file lama, bukan file baru). |
| 2 | `27cd0ab` Extract useAuthSession hook | `hooks/useAuthSession.ts` — state `userId`, `userEmail`, `isLoggingOut` + fungsi `handleLogout`. Effect get-user-or-redirect-to-login pindah ke hook; effect pemanggilan 5 loader (`loadWorkspaces`, `loadConversations`, `loadUsage`, `loadLearningProfile`, `loadSkills`) **tetap di `page.tsx`**, sekarang di-trigger oleh `useEffect(..., [userId, ...])` terpisah. |
| 3 | `7df0e2b` Extract useWorkspaces hook | `hooks/useWorkspaces.ts` — state `workspaces`, `selectedWorkspaceId`, `newWorkspaceName`, `isCreatingWorkspace` + fungsi `loadWorkspaces`, `createWorkspace`. Menerima `setHistoryError` sebagai parameter callback (bukan hook error sendiri). |
| 4 | `8763060` Extract useKnowledgeBase hook | `hooks/useKnowledgeBase.ts` — 8 state knowledge (`knowledgeSources`, `isKnowledgeAdmin`, `isLoadingKnowledge`, `isUploadingKnowledge`, `knowledgeTitle`, `knowledgeCategory`, `knowledgeMessage`, `knowledgeError`) + `hasLoadedKnowledgeRef` + fungsi `loadKnowledge`, `handleKnowledgeUpload`. Effect lazy-load (trigger saat tab Settings > Knowledge Base dibuka) **tetap di `page.tsx`** karena depend ke `activeSettingsTab`/`isSettingsOpen` (domain settings-shell, belum diekstrak) — hook cuma expose `loadKnowledge`, `isLoadingKnowledge`, `hasLoadedKnowledgeRef`. |
| 5 | `bafe39a` Extract useUsage hook | `hooks/useUsage.ts` — state `usageSnapshot`, `usageError` + fungsi `loadUsage` + derived `currentTierLabel`, `allowedModels`, `currentPlan`, `hasMessageQuota`, `hasUploadQuota`. Ketiga call site `loadUsage()` (initial load, setelah upload dokumen, setelah `sendMessage` selesai) **tetap di `page.tsx`**, tidak berubah. `loadLearningProfile`/`saveLearningProfile` **sengaja tidak diikutkan** — meski keduanya juga menulis balik ke `selectedModel`/`selectedSkillId`, itu berdasarkan `learningProfile` (data preferensi tersimpan), bukan status kuota/tier, jadi tetap domain `useUserMemory` (langkah 8). Kopling ke `selectedModel`/`selectedSkillId` diselesaikan dengan hook menerima `skillsRef`, `setSelectedModel`, `setSelectedSkillId` sebagai parameter (bukan import hook lain) — lihat catatan kopling di bawah. |
| 6 | `88afa43` Extract useSkills hook + split useUsage | `hooks/useSkills.ts` (baru) — state `skills`, `skillsLoading`, `selectedSkillId`, ref `skillsRef` + fungsi `loadSkills`, `selectSkill` + derived `selectedSkill`, `selectedSkillBadge`, plus effect fallback-pilih-skill-pertama, sync `skillsRef`, dan persist `localStorage` key `ai-mu-study-mode`. Menerima `tier` & `setIsStudyModeMenuOpen` sebagai parameter. **`hooks/useUsage.ts` dipecah** jadi `useUsage()` (lapis data murni, zero params) + `applyUsageConstraints()` (fungsi biasa, bukan hook, menerima `skillsRef`/setter sebagai parameter) — ini menyelesaikan circular dependency asli antara `useUsage` butuh `skillsRef` dan `useSkills` butuh `tier` dari `useUsage`. Urutan final: `useUsage()` → `useSkills(tier)` → compose ulang `loadUsage`. Known issue baru ditemukan (bukan regresi, dikonfirmasi pre-existing via `git stash`): skill revert ke default setelah reload — lihat bagian "Known Issues". |
| 7 | `da84ade` Extract useModelSelection hook | `hooks/useModelSelection.ts` (baru) — state `selectedModel`, `isModelMenuOpen`, `isUpgradeOpen`, `upgradeTargetModel` + fungsi `selectModel`, `openUpgradeModal` + derived `selectedModelInfo`, `upgradePlan`. Tidak ada effect yang ikut pindah (beda dari `useSkills`). Terima `allowedModels` (dari `useUsage`) sebagai parameter. Wrapper `loadUsage` di `page.tsx` dikomposisi ulang supaya `applyUsageConstraints` menerima `setSelectedModel` dari hook baru ini (bukan lagi `useState` lokal) — `applyUsageConstraints` sendiri di `hooks/useUsage.ts` tidak berubah. JSX modal upgrade (jauh dari definisi hook) dan 3 titik cross-toggle inline antara model-menu/study-mode-menu tetap jalan tanpa edit karena semua variabel di-destructure ke scope `page.tsx` yang sama. |
| 8 | `784d84d` Extract useUserMemory hook | `hooks/useUserMemory.ts` (baru) — state `learningProfile`, `profileDraft`, `favoriteSubjectsDraft`, `isSavingProfile`, `profileError`, `profileSavedMessage` + fungsi `loadLearningProfile`, `updateProfileDraft`, `saveLearningProfile` + derived `profileLabel`, plus effect sync tema. Ekstraksi murni — `loadLearningProfile` awalnya tetap pakai `skillsRef.current` persis seperti sebelumnya (bug known issue #1 diikutkan apa adanya, sengaja tidak diperbaiki di commit ini, dikonfirmasi via test reproduksi ulang sebelum lanjut). Bug fix menyusul di commit terpisah `af4d82f` — lihat "Known Issues" di bawah, sudah **RESOLVED**. |
| 9-10 | `8b1006d` Extract useAttachments + useConversations hooks | **Digabung jadi satu commit** atas instruksi eksplisit user untuk mempercepat sisa langkah (lihat "Mode Percepatan" di bawah). `hooks/useAttachments.ts` (baru) — state `uploadedAttachments`, `recentAttachments`, `documentText`, `documentStatus`, `documentError`, `composerNotice`, `isAttachMenuOpen` + ref `documentTextRef`, `uploadKeysInFlightRef`, `uploadFilesByAttachmentIdRef` + semua fungsi attachment/upload + effect hydrate/persist `recentAttachments` ke `localStorage`. Terima `userId`, `hasUploadQuota`, `loadUsage` (dari `useUsage`) sebagai parameter. `hooks/useConversations.ts` (baru) — state `conversations`, `searchConversations`, `activeConversationId`, `isLoadingConversations`, `renamingConversationId`, `renameValue`, `chatSearch` + fungsi `loadConversations`, `renameConversation`, `deleteConversation`, `toggleConversationPin`, `updateConversationWorkspace` + derived `visibleConversations`/`activeConversation` + effect debounced search. Terima `skillsRef`, `setHistoryError`, `resetMemory` (masih fungsi hoisted biasa di `page.tsx`, langkah 11) sebagai parameter — `historyError` sendiri TETAP global di `page.tsx` (keputusan lama dikonfirmasi ulang), `conversationGroups` TETAP di `page.tsx` (gabung dengan `workspaces` dari hook lain). Tidak ada circular dependency ditemukan antar kedua hook baru maupun ke 6 hook yang sudah ada. |
| 11 | `30d7bc7` Extract useChatSession hook (branch `refactor/step-11-use-chat-session`) | `hooks/useChatSession.ts` (baru, ~26 parameter constructor, hook terbesar) — state `messages`, `input`, `isSending`, `isAwaitingFirstChunk`, `sharePreview` + ref `messagesEndRef` (`activeRequestRef`/`scrollFrameRef` jadi internal-only, tidak di-return karena tidak dipakai JSX) + fungsi `sendMessage` (~390 baris), `continueAnswer`, `loadConversation`, `createConversation`, `resetChatSessionState`, `exportActiveChatMarkdown`, `openSharePreview` (`getActiveChatMarkdown` jadi internal-only) + effect auto-scroll & abort-on-unmount. **Circular dependency (dicatat di langkah 9-10) diselesaikan**: bukan opsi (a)/(b) yang diantisipasi sebelumnya, tapi solusi lebih ringan — `resetMemory` dipindah dari parameter constructor `useConversations` jadi parameter call-time opsional `deleteConversation(conversationId, resetMemory?)`, dipanggil dari 1 titik JSX (`deleteConversation(conversation.id, resetMemory)`). `useConversations` jadi tidak butuh apa pun dari `useChatSession` saat construct, jadi urutan final: `useConversations()` → `useChatSession(...)` → `resetMemory` dikomposisi ulang di `page.tsx` dari `resetChatSessionState()` + setter `useConversations` + `resetDocumentState()` dari `useAttachments`. Test manual checklist lengkap (bukan smoke test) termasuk titik krusial: hapus percakapan AKTIF (resetMemory ter-trigger benar) vs hapus percakapan TIDAK aktif (composer tidak terganggu), upload dokumen/gambar lalu kirim pesan yang mereferensikannya (AI benar mendeskripsikan isi gambar test), reload berulang. Test abort-mid-stream di-skip (bukan bug — ref `activeRequestRef` internal-only, tidak diubah refactor, tidak ada tombol stop user-facing di UI, trigger abort cuma re-send-menyusul atau unmount). |
| 12 | `6eb1913` Extract useSettingsPanel hook | `hooks/useSettingsPanel.ts` (baru, shell tipis) — state `isSettingsOpen`, `activeSettingsTab`, `settingsDataMessage` + fungsi `openSettings`, `openLearningProfile`, `deleteAllChatHistory`, `exportChatHistoryPlaceholder` + effect lazy-load Knowledge Base tab (akhirnya bisa pindah dari `page.tsx` sekarang, karena `isSettingsOpen`/`activeSettingsTab` sudah bukan lagi "domain settings-shell belum diekstrak" seperti dicatat di langkah 4). Terima `resetMemory` & `exportActiveChatMarkdown` dari `useChatSession`, `setConversations` dari `useConversations`, beberapa setter dari `useUserMemory`/`useKnowledgeBase` sebagai parameter — tidak ada circular dependency (dipanggil paling akhir, tidak ada hook lain yang butuh apa pun darinya). Type `SettingsTab` dipindah dari lokal `page.tsx` ke `lib/mappers/types.ts` (pola sama seperti `Message`/`Conversation`/dst di langkah 1) karena sekarang dibutuhkan di 2 tempat. `supabase` client lokal `page.tsx` (`useMemo`) dihapus karena satu-satunya pemakainya (`deleteAllChatHistory`) sudah pindah ke hook. Dieksekusi dengan pola dipersingkat (rencana ringkas, bukan plan-mode penuh) atas instruksi user karena risiko/ambiguitas rendah — dikonfirmasi tidak ada temuan yang mengubah itu. Test manual: buka Settings, pindah ke tab Knowledge Base (verifikasi effect lazy-load pindah dengan benar, data knowledge source asli termuat), tutup Settings — reload bersih, zero console error di seluruh sesi. |

Semua langkah di atas: `tsc --noEmit` bersih, `npm run build` sukses, `npm run lint` bersih, dan sudah di-test manual di browser (login/logout, buat workspace baru, upload & hapus knowledge source test; langkah 5: badge tier, pengurangan kuota pesan/upload harian, tombol disable saat kuota habis, auto-reset model di luar allowed list, tampilan tab Settings > Subscription; langkah 6: dropdown skill, badge & lock icon per tier, redirect ke `/plans` untuk skill terkunci, fallback pilih skill pertama, regresi kuota-habis; langkah 7: dropdown model, lock icon + modal upgrade untuk model terkunci, cross-toggle model-menu/study-mode-menu, regresi kuota-habis, load percakapan lama, save/load Learning Profile; langkah 8: reproduksi bug known issue #1 dulu (belum diperbaiki di commit ini), lalu setelah `af4d82f` — reload berulang (4x) mempertahankan skill terpilih, load percakapan lama dengan skill berbeda tidak meng-corrupt `localStorage`, Settings > General > Default study mode tersimpan & bertahan setelah reload; langkah 9-10 (smoke test, bukan checklist detail — sesuai mode percepatan): load percakapan lama menampilkan pesan yang benar, chat search memfilter dengan benar, attach menu terbuka dengan section recent files; langkah 11: lihat detail di atas, checklist paling lengkap karena hook paling berisiko; langkah 12: smoke test tab settings + lazy-load knowledge base, risiko rendah sesuai klasifikasi user).

## Mode Percepatan (mulai Langkah 9)

Atas instruksi user, langkah 9 & 10 dikerjakan dengan pola dipersingkat karena keterbatasan waktu — **beda dari pola langkah 1-8**:
- Langkah 9 & 10 digabung jadi satu sesi kerja/rencana (bukan plan-review-eksekusi terpisah per langkah).
- Rencana ringkas dulu (poin-poin kunci, bukan analisis detail) baru langsung eksekusi tanpa nunggu approve — kecuali ditemukan risiko tinggi/ambigu setara kasus race condition langkah 8 (tidak ditemukan di langkah 9-10).
- Test lokal secukupnya (smoke test), bukan checklist 6-8 poin detail.
- **Kedua hook di-commit jadi SATU commit** (`8b1006d`), bukan 2 commit terpisah seperti awalnya diminta — pemanggilan kedua hook ditulis berdekatan dalam satu blok komposisi di `page.tsx` pada satu sesi edit, sehingga memisahkannya jadi 2 commit yang MASING-MASING tetap bisa di-compile sendiri tidak praktis tanpa mengulang edit secara berurutan. Dilaporkan sebagai deviasi ke user, bukan dilakukan diam-diam.
- Push langsung ke branch gabungan, skip Vercel Preview kecuali ada temuan mencurigakan.
- **Langkah 11 kembali ke pola lengkap** (plan dulu → review → eksekusi, checklist manual lengkap) karena scope & risiko paling besar. **Langkah 12** ternyata dieksekusi dengan pola dipersingkat lagi (rencana ringkas → eksekusi langsung kalau tidak ada risiko/ambiguitas → satu laporan di akhir) atas instruksi user, karena setelah langkah 11 selesai, langkah 12 terbukti kecil & rendah risiko (shell tipis, tidak ada circular dependency baru).

Detail eksekusi langkah 11 & 12 (state/fungsi/effect yang pindah, solusi circular dependency yang benar-benar dipakai, hasil test manual) ada di baris tabel commit 11 & 12 di atas — bukan diulang di sini.

## Catatan Kopling Penting

**`loadUsage`, `loadLearningProfile`, dan `saveLearningProfile` menulis balik ke `selectedModel` dan `selectedSkillId`** (reset ke default kalau nilai saat ini di luar allowed list untuk tier/paket user). Ini kopling lintas-domain yang sudah ada dari awal, bukan sesuatu yang diperkenalkan migrasi ini.

**Kondisi final setelah langkah 5-7** (bukan lagi asumsi transisional — ini yang benar-benar terpasang di kode sekarang):
- `hooks/useUsage.ts` punya 2 export: `useUsage()` (lapis data murni, **zero parameter**, isinya cuma fetch + state `usageSnapshot`/`usageError` + derived `currentTierLabel`/`allowedModels`/`currentPlan`/`hasMessageQuota`/`hasUploadQuota`) dan `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)` (**fungsi biasa, bukan hook** — tidak makai `useState`/`useEffect` sendiri, cuma menerima setter dari domain lain lewat parameter).
- Urutan pemanggilan hook final di `page.tsx`: **`useUsage()` dulu** (tidak butuh apa-apa dari hook lain) → `usageSnapshot?.tier` & `allowedModels` tersedia dari return-nya → `useSkills(tier, setIsStudyModeMenuOpen)` dan `useModelSelection(allowedModels)` (keduanya independen satu sama lain, urutan relatif bebas) → baru `page.tsx` mengomposisi ulang wrapper `loadUsage` yang memanggil `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)` memakai `skillsRef`/`setSelectedSkillId` dari `useSkills` dan `setSelectedModel` dari `useModelSelection`.
- Prinsip di balik desain ini: kalau hook A butuh menulis ke state yang dimiliki hook B, **A tidak boleh menerima setter B sebagai parameter constructor-nya sendiri** (itu yang menciptakan circular dependency di langkah 6). Sebagai gantinya, A dipecah jadi lapis data (zero/minimal parameter) + fungsi biasa yang menerima setter sebagai argumen saat **dipanggil**, bukan saat hook itu sendiri di-invoke. Komposisi lintas-domain terjadi di `page.tsx` (lewat wrapper seperti `loadUsage`), bukan di dalam hook manapun.

Aturan yang dipakai sejauh ini dan **harus tetap dipakai** di langkah 11-12:
- **Jangan** hook A meng-import hook B langsung (hindari circular dependency dan hook yang diam-diam tahu terlalu banyak tentang domain lain).
- Kalau hook A perlu memicu efek di domain B, **A menerima fungsi/setter dari B sebagai parameter/callback**, dipanggil dari `page.tsx` yang menggabungkan semuanya. Pola ini sudah dipakai di `useWorkspaces(setHistoryError)`, `useKnowledgeBase()` (yang effect-nya sengaja tidak ikut pindah karena butuh state dari domain settings-shell), `useSkills(tier, setIsStudyModeMenuOpen)`, dan `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)`.
- Kalau ternyata ada kebutuhan **dua arah** (hook A butuh data dari B, B butuh setter dari A) seperti kasus `useUsage`/`useSkills` di langkah 6: pecah salah satu hook jadi lapis data (zero/minimal parameter, bisa dipanggil kapan saja) + fungsi komposisi terpisah yang dipanggil belakangan di `page.tsx` — jangan paksakan urutan pemanggilan untuk "menyelesaikan" circular dependency asli, itu tidak akan bisa.

## Known Issues

Bug yang ditemukan selama migrasi tapi **bukan** disebabkan oleh migrasi itu sendiri, dicatat di sini supaya tidak hilang dan (kalau belum) bisa ditangani di langkah yang scope-nya memang cocok.

### 1. Skill terpilih revert ke default ("Cambridge Tutor") setelah reload halaman — ✅ RESOLVED di `af4d82f`

**Gejala:** Ganti skill via dropdown Study Mode (mis. ke "Islamic Teacher"), lalu reload halaman penuh (F5) — skill yang tampil kembali ke default platform ("Cambridge Tutor"), bukan skill yang barusan dipilih. Bug ini flaky/race-timing-dependent (reproduce ~4 dari 5 reload saat diverifikasi, bukan 100% setiap saat).

**Root cause asli (dugaan awal, TIDAK LENGKAP):** race antara `loadLearningProfile(userId)` dan `loadSkills(userId)` yang dipanggil bersamaan lewat `Promise.all([...])` — `loadLearningProfile` resolve skill dari `localStorage` via `skillsRef.current` yang mungkin masih `[]` karena belum sempat sinkron dari `loadSkills`.

**Percobaan fix pertama (commit terpisah, TERBUKTI TIDAK CUKUP, sudah di-revert sebelum push manapun):** mengubah `loadSkills` supaya return `Skill[]` langsung dan menyusun ulang `Promise.all` supaya `loadLearningProfile` dipanggil setelah `loadSkills` selesai, pakai array yang baru di-fetch alih-alih `skillsRef.current`. Setelah di-test ulang dengan benar (percobaan pertama sempat salah klik di test tooling, menciptakan false positive), bug **masih reproduce** — root cause asli ternyata cuma separuh cerita.

**Root cause sebenarnya (dikonfirmasi via testing berulang):** `hooks/useSkills.ts` punya efek generik yang mem-persist `selectedSkillId` ke `localStorage` setiap kali berubah — **termasuk** saat efek fallback (yang otomatis memilih skill default saat `skills` sudah termuat tapi `selectedSkillId` masih `null`) yang menembak. Karena `loadSkills` (tabel kecil) hampir selalu selesai duluan dibanding `loadWorkspaces`/`loadConversations`/`loadUsage` (yang berjalan bersamaan di `Promise.all` yang sama), ada jendela waktu nyata di mana `skills` sudah terisi tapi `selectedSkillId` masih `null` — efek fallback menembak, memilih "Cambridge Tutor", dan **efek generik itu langsung menimpa `localStorage`** dengan id Cambridge Tutor SEBELUM `loadLearningProfile` sempat baca nilai asli yang tersimpan. Begitu `loadLearningProfile` akhirnya jalan, ia membaca `localStorage` yang sudah ter-corrupt dan "dengan benar" resolve ke Cambridge Tutor — jadi ini bukan cuma soal `loadLearningProfile` baca data basi, tapi `localStorage`-nya sendiri yang ke-corrupt duluan oleh sumber lain.

**Fix final (`af4d82f`):**
- `hooks/useSkills.ts`: efek generik persist-ke-localStorage **dihapus**. `localStorage.setItem("ai-mu-study-mode", ...)` sekarang hanya dipanggil eksplisit di dalam `selectSkill` (aksi pilih skill yang disengaja oleh user). Efek fallback & `applyUsageConstraints` (re-validasi tier) tetap mengubah `selectedSkillId` di memory (supaya UI tetap menampilkan sesuatu yang masuk akal), tapi tidak lagi menimpa preferensi yang tersimpan.
- `hooks/useUserMemory.ts`: `saveLearningProfile` (aksi simpan eksplisit lain, dari Settings > Personalization/General) **juga** perlu persist eksplisit sendiri — kalau tidak, perubahan default study mode di Settings akan diam-diam tidak berlaku lagi di reload berikutnya karena `localStorage` lama (dari quick-switch sebelumnya) akan tetap menang. `loadLearningProfile` menerima `currentSkills` sebagai parameter (bukan lagi `skillsRef.current`) — bagian dari percobaan fix pertama yang tetap dipertahankan karena memang benar secara terpisah.
- `applyUsageConstraints` (re-validasi tier turun) dan `loadConversation` (restore skill kontekstual per-percakapan) **sengaja tidak** dibuat persist — keduanya bukan preferensi permanen user, cuma penyesuaian in-memory/kontekstual.

**Verifikasi:** reload berulang (4x berturut-turut) mempertahankan skill terpilih; load percakapan lama dengan skill berbeda tidak meng-corrupt `localStorage` (dan reload sesudahnya tetap kembali ke preferensi asli, bukan skill kontekstual percakapan itu); Settings > General > Default study mode tersimpan dan bertahan setelah reload.

**Regresi susulan ditemukan & diperbaiki (masih di scope fix yang sama, commit terpisah):** setelah fix di atas, `loadLearningProfile` jadi SELALU berhasil resolve skill dari `localStorage` (sebelumnya nyaris selalu gagal karena bug ref-staleness) dan SELALU jalan setelah `Promise.all` (termasuk `loadUsage()` yang memanggil `applyUsageConstraints` untuk validasi tier). Akibatnya, kalau `localStorage` menyimpan id skill yang sudah tidak diizinkan tier user (mis. user turun tier, atau skill Premium lama tersimpan), `loadLearningProfile` — yang sengaja *ungated* By design (validasi tier didelegasikan ke `applyUsageConstraints`) — jadi resolve UNGATED PALING AKHIR, menimpa hasil validasi tier yang sudah dilakukan `applyUsageConstraints` lebih dulu di `Promise.all` yang sama. Efeknya: skill Premium yang sudah terkunci tampil sebagai "terpilih" di header (dikonfirmasi via simulasi: set `localStorage['ai-mu-study-mode']` ke id skill `min_tier: kader_pintar` pada akun Free, reload → header tampil "OSN Coach · PREMIUM"). **Bukan celah keamanan** — `sendMessage` independen re-validasi via `resolveAllowedSkill(selectedSkillId, tier, skills)` sebelum benar-benar memakai skill, jadi tidak ada system prompt Premium yang benar-benar terpakai, cuma tampilan UI yang salah/menyesatkan.

**Fix:** `loadLearningProfile` sekarang menerima `currentTier` sebagai parameter call-time tambahan (BUKAN lewat closure di `useCallback` — kalau `tier` masuk dependency array `loadLearningProfile`, function reference-nya berubah tiap `usageSnapshot` update (yaitu tiap abis kirim pesan), dan karena `loadLearningProfile` ada di dependency array effect initial-load, itu akan MEMICU ULANG seluruh initial load setiap kirim pesan — regresi jauh lebih parah). Untuk itu, wrapper `loadUsage` di `page.tsx` diubah supaya return `snapshot`-nya (pola yang sama seperti `loadSkills`), dan effect initial-load men-destructure `usageSnapshotResult?.tier` langsung dari hasil `Promise.all`, dioper ke `loadLearningProfile(userId, fetchedSkills, usageSnapshotResult?.tier)`. Di dalam `loadLearningProfile`, resolve sekarang lewat `resolveAllowedSkill(..., currentTier, currentSkills)` — sama seperti pola yang sudah dipakai `saveLearningProfile`.

**Verifikasi:** simulasi locked-skill-in-localStorage pada akun Free (id skill `min_tier: kader_pintar` disuntik manual ke `localStorage`) → reload → header dengan benar fallback ke "Cambridge Tutor · Basic" (dikonfirmasi 2x reload berturut-turut, dengan `usageSnapshot` yang genuinely terisi tier `"free"` dari respons `/api/usage` yang sukses, bukan cuma fallback `undefined`-diperlakukan-`"free"`). Regresi check: skill yang memang diizinkan (Islamic Teacher) tetap persist dengan benar lintas reload (2x reload berturut-turut) setelah perubahan ini.

## Pola Kerja yang Terbukti Jalan (dipakai sepanjang langkah 1-12)

1. Baca kode existing dulu (state + fungsi + effect yang relevan) sebelum bikin hook baru — jangan asumsi dari ingatan.
2. Buat `hooks/useX.ts` — pindahkan state/fungsi/effect (effect ikut pindah hanya kalau tidak depend ke domain lain yang belum diekstrak).
3. Edit `page.tsx`: ganti `useState`/fungsi lama dengan satu pemanggilan hook, hapus definisi lama, tambah import.
4. Cek import yang jadi tidak terpakai (fungsi lib, tipe lokal) — hapus kalau memang dead code akibat langsung dari ekstraksi ini (bukan cleanup di luar scope).
5. `npx tsc --noEmit` → harus bersih.
6. `npm run lint` → perbaiki warning yang muncul akibat langsung dari ekstraksi (biasanya `react-hooks/exhaustive-deps` untuk ref yang sekarang datang dari hook eksternal — aman ditambah ke dependency array, tidak mengubah behavior).
7. `npm run build` → harus sukses.
8. Test manual di browser, **spesifik ke fitur yang barusan diekstrak** (bukan smoke test umum) — kalau dev server sempat error "defined multiple times" atau sejenisnya padahal file di disk sudah benar, itu cache Turbopack basi: `rm -rf .next` lalu restart dev server.
9. Commit terpisah per hook (pesan commit format: `Extract useX hook from page.tsx (no behavior change)` atau sesuai konteks kalau ada perubahan tambahan seperti dead-code removal).
10. Baru lanjut ke hook berikutnya.

## Setelah Langkah 12 Selesai

1. **Pecah JSX**: `page.tsx` yang tersisa (didominasi JSX render tree) dipecah jadi komponen presentational — kandidat yang sudah teridentifikasi dari analisis awal: `<ModelMenu>`, `<SkillMenu>`, `<AccountMenu>` (saat ini di-JSX 2× untuk desktop/mobile, bagus disatukan), `<AttachMenu>`, `<UpgradeModal>`, `<SettingsModal>` (shell + per-tab), `<SharePreviewModal>`, sidebar conversation list, composer.
2. **Baru integrasikan desain visual baru** dari `stitch-reference/` — di titik ini state/logic sudah terisolasi di hooks jadi mengganti tampilan (JSX + styling) jauh lebih aman, tidak akan mengubah behavior secara tidak sengaja.

## Langkah 13: Extract `<Sidebar>` component + restyle Stitch (branch `refactor/sidebar-component`, commit `e60ec0b`)

Langkah pertama JSX-extraction pasca hook-migration, sekaligus komponen pertama yang menerima integrasi visual dari `stitch-reference/`. Dikerjakan dengan pola lengkap (plan → review → eksekusi → checklist manual), bukan mode dipersingkat, karena prop count terbesar dan risiko tertinggi salah kutip variabel (sesuai prediksi `ARCHITECTURE_SUMMARY.md` bagian 3).

**Ekstraksi:** `components/Sidebar.tsx` (baru) — memindahkan `<aside>` gabungan (baris 518-860 versi `page.tsx` sebelum langkah ini) apa adanya: logo+toggle, tombol "+ Obrolan baru" + workspace switcher + create-workspace form, search percakapan, daftar percakapan (grouped by workspace, rename/pin/delete inline, per-conversation workspace reassign), account menu popover + trigger row di bagian bawah. Murni presentational — **tidak ada state yang jadi local ke komponen**; semua tetap dimiliki hooks di `page.tsx` dan lewat props, karena dua state di dalamnya dipakai bersama komponen lain yang belum diekstrak: `isAccountMenuOpen` (dipakai juga oleh popover account menu duplikat di TopBar) dan `chatSearch` (dipakai juga oleh `MobileToolbar`). `router.push("/plans")` dipanggil lewat `useRouter()` yang di-invoke langsung di dalam `Sidebar.tsx` (bukan di-prop-drill dari `page.tsx`).

**Restyle visual:** mengadopsi penuh tema hijau tua Stitch dari `stitch-reference/workspace_chat_ai_muhammadiyah_standardized/code.html` (panel "Workspace List Sidebar", baris 144-211) — dikonfirmasi ke user lewat AskUserQuestion sebelum eksekusi (opsi "adopsi penuh" dipilih, bukan tetap tema terang atau kompromi). Background `<aside>` dari `bg-[#eef8f1]` → solid `bg-[#006837]`, teks putih; input (search, workspace select/create) dari card putih → translucent `bg-white/10`; item percakapan aktif dari `ring-1 ring-[#d8eadf]` → `border-l-4 border-[#fdc003] bg-black/20` (amber, bukan reuse warna badge tier `#fff4d8`/`#8a5a00` karena kontrasnya tidak cukup di atas hijau tua); popover account menu **tetap kartu putih** (tidak direstyle gelap), konsisten dengan Level-2 elevation Stitch sendiri dan popover/modal lain di app. Semua warna baru pakai arbitrary Tailwind value (`bg-[#006837]`, dst), konsisten dengan konvensi existing, tidak menambah token baru ke `tailwind.config`. Icon system tetap pakai `Icon`/`SparkIcon` existing (`components/icons.tsx`), tidak memperkenalkan Material Symbols font dari mockup Stitch.

**Di luar scope (sengaja tidak disentuh):** layout 4-panel Stitch (Icon Rail 60px + Knowledge Sidebar kanan 240px) — Sidebar tetap satu `<aside>` gabungan seperti sebelumnya, hanya reskin. Duplikasi account menu Sidebar/TopBar tidak dikonsolidasi. Tombol "Alihkan sidebar" (`aria-label="Alihkan sidebar"`) **tidak punya `onClick` — dead button, pre-existing sebelum ekstraksi ini**, dipindah apa adanya, dilaporkan sebagai temuan terpisah (belum diperbaiki).

**Verifikasi:** `npx tsc --noEmit`, `npm run lint`, `npm run build` — ketiganya bersih (dijalankan 2×, sebelum dan sesudah cleanup scratch test route). Test manual: dilakukan pada **akun live pengguna sendiri** (bukan akun test terpisah — OTP login tidak bisa disimulasikan otomatis oleh agent karena diblokir permission classifier sebagai "real-world transaction"; sesi sudah authenticated di browser preview, kemungkinan user login manual sendiri setelah percobaan fill agent diblokir). Dikonfirmasi lewat DOM inspect + accessibility snapshot (bukan screenshot — `preview_screenshot` timeout terus-menerus di sesi ini, isu tooling, bukan isu kode; diverifikasi lewat `preview_inspect`/`preview_snapshot` sebagai gantinya): search filter conversation jalan, klik conversation me-load chat asli, item aktif dapat aksen border amber `#fdc003` yang benar, pin/unpin toggle jalan (diuji lalu di-revert supaya data akun tidak berubah), form rename muncul dengan benar (tidak disubmit, supaya judul asli tidak berubah), account menu popover Sidebar & TopBar terbukti share state yang sama (buka dari TopBar → popover Sidebar ikut terbuka), reload bersih tanpa console error. Interaksi yang mengubah data permanen (create workspace, submit rename, delete, logout) **sengaja tidak dieksekusi** terhadap akun live untuk menghindari mutasi data produksi tanpa perlu — coverage CRUD penuh (semua 5 kondisi tier `free`/`kader_pintar`/`muallim_pro`/`dakwah_digital`/`sinergi_ranting` untuk badge trigger account menu, termasuk) diuji lewat harness scratch terpisah (`app/dev-sidebar-preview-scratch/`, dihapus sebelum commit, tidak masuk diff) dengan mock props.

**Belum diuji end-to-end secara penuh:** karena testing dilakukan lewat sesi authenticated yang sudah ada (bukan agent yang menjalankan login), belum ada verifikasi baris-demi-baris terhadap kemungkinan sesi ini adalah sesi lama/cached yang bukan representasi murni "user baru login". Rekomendasi: user cek ulang di Vercel Preview dengan login normal sebelum merge.

## Langkah 14: Extract `<TopBar>` component, TANPA restyle warna (branch `refactor/topbar-component`, commit `e491e4f`)

Kandidat kedua dari daftar JSX-extraction (`ARCHITECTURE_SUMMARY.md` bagian 3). Beda penting dari langkah 13: **tidak ada restyle Stitch untuk warna di langkah ini** — keputusan dikonfirmasi user lewat AskUserQuestion setelah ditemukan bahwa hampir semua hex value di `<header>` (`#008d54`, `#009252`, `#d8eadf`, `#d9e9df`, `#eef8f1`, `#f7fbf8`, `#fff4d8`/`#8a5a00`) adalah token yang dipakai **berulang ratusan kali** di ChatArea/Composer/MobileToolbar/modal yang belum diekstrak (contoh: `#008d54` muncul 39× di seluruh `page.tsx`, cuma 10× di dalam TopBar) — beda dari Sidebar yang jadi solid gelap (otomatis "terpisah" secara visual dari sekitarnya), restyle TopBar sendirian akan menciptakan jahitan warna di batas TopBar↔ChatArea. User memilih **ekstrak dulu, warna tetap sama persis**, restyle Stitch ditunda sampai ChatArea/Composer/MobileToolbar juga diekstrak (supaya palette diseragamkan sekaligus). Referensi Stitch yang dicek (`workspace_chat_.../code.html` baris 212-240, dan header serupa di `docs_ai_muhammadiyah_workspace_standardized/code.html` baris 182-212 mewakili 4 tools baru) juga terbukti sudah terang secara struktur (`bg-surface-container-lowest`) — jadi tidak ada isu terang-vs-gelap seperti Sidebar, tapi strukturnya (judul workspace + tab nav Chat/Docs/Tasks/Sheets/Canvas) tidak ada analognya di TopBar kita (dropdown model/skill interaktif + account avatar) — gap arsitektur terpisah, dicatat bukan diselesaikan sesi ini.

**Ekstraksi:** `components/TopBar.tsx` (baru) — memindahkan `<header>` (baris 558-834 versi `page.tsx` sebelum langkah ini) 1:1 byte-identik (termasuk semua className): dropdown model AI (badge GPT, lock+`getLockedModelRequirement` utk model terkunci), dropdown skill/study-mode (badge tier via `getSkillBadge`, lock+`getLockedSkillRequirement` utk skill terkunci, gating via `canAccessTier`), cross-toggle antar 2 dropdown, baris pin/export/share (`hidden sm:flex`, cuma tampil kalau `activeConversation` ada), badge tier (`hidden sm:inline-flex`), tombol+popover account menu (popover **`md:hidden`** — cuma tampil mobile; equivalent-nya di desktop adalah popover `Sidebar`, karena `Sidebar` sendiri `hidden md:flex` — split responsif 1-state-2-popover ini **dipertahankan apa adanya**, sesuai keputusan eksplisit user TopBar tetap duplikasi terpisah dari Sidebar, bukan dikonsolidasi).

**State ownership** (pengecekan sama seperti Sidebar sebelum putuskan tetap di `page.tsx` vs local): `isAccountMenuOpen`/`setIsAccountMenuOpen` tetap di `page.tsx` (shared literal dengan `Sidebar`, dikonfirmasi via `grep` — cuma 1 `useState` di baris 96, di-pass ke `Sidebar` baris 539 dan `TopBar` baris 576, prop name sama persis). `isStudyModeMenuOpen`/`setIsStudyModeMenuOpen` tetap di `page.tsx` — bukan karena dipakai komponen JSX lain, tapi karena `setIsStudyModeMenuOpen` di-thread sebagai parameter constructor `useSkills(tier, setIsStudyModeMenuOpen)` (dibutuhkan SAAT hook di-construct, sebelum `TopBar` di-render — tidak bisa jadi local state komponen). `modelOptions` (const module-level di `page.tsx`, bukan state) tetap di `page.tsx` karena dipakai juga oleh Composer (belum diekstrak) — di-pass sebagai prop `PlanModelId[]`, bukan diduplikasi ke `TopBar.tsx`. `activeConversation`/`toggleConversationPin`/`exportActiveChatMarkdown`/`openSharePreview` dipakai juga di baris pin/export/share `MobileToolbar` — bukan isu ownership (data/fungsi hook, bukan state, wajar di-pass ke 2 komponen).

Fungsi murni (`getModelProviderLabel`, `getLockedModelRequirement`, `getLockedSkillRequirement` dari `lib/chat/selection-labels`; `canAccessTier` dari `lib/skills`) dipindah importnya ke `TopBar.tsx` dan **dihapus** dari `page.tsx` (dikonfirmasi lewat grep tidak ada pemakaian lain di `page.tsx` sebelum dihapus — cek eksplisit diminta user). `getSkillBadge`/`modelCatalog` **tetap** diimpor di `page.tsx` juga karena masih dipakai di `UpgradeModal`/Composer (belum diekstrak) — jadi diimpor di kedua file. `router.push("/plans")` lewat `useRouter()` yang di-invoke sendiri di `TopBar.tsx` (pola sama seperti `Sidebar.tsx`); `page.tsx` sendiri masih perlu `useRouter()`-nya sendiri karena `router` masih dipakai di `UpgradeModal` (baris ~1348, belum diekstrak).

**Verifikasi:** `npx tsc --noEmit`, `npm run lint`, `npm run build` — ketiganya bersih (dijalankan 2×). Test manual: sesi browser preview **kembali ke halaman login** (tidak authenticated lagi) di awal sesi ini — sesuai instruksi eksplisit user, agent **tidak** mencoba login/isi OTP sendiri. Karena itu, testing fungsional penuh dilakukan lewat **harness scratch terpisah** (`app/dev-topbar-preview-scratch/`, mock props, dihapus sebelum commit, tidak masuk diff): switch 5 kondisi tier (`free`/`kader_pintar`/`muallim_pro`/`dakwah_digital`/`sinergi_ranting`, termasuk label terpanjang "SINERGI RANTING" dikonfirmasi tidak overflow), buka/pilih dari dropdown model (lock+"Premium"+`getLockedModelRequirement` benar utk tier Free, semua unlocked utk Sinergi Ranting), buka/pilih dari dropdown skill (sama, via `getLockedSkillRequirement`+`canAccessTier`), cross-toggle 2 dropdown (buka salah satu otomatis tutup lainnya, dikonfirmasi), klik pin/export/share (masing-masing trigger handler yang benar), account menu popover mobile (resize ke 375px, semua 5 item — Upgrade plan/Learning Profile/Usage-quota/Settings/Logout — render & trigger handler yang benar, popover auto-close setelah klik item). **Sinkronisasi `isAccountMenuOpen` antara `Sidebar` dan `TopBar` di halaman nyata TIDAK bisa diuji end-to-end** (perlu sesi live, tidak tersedia) — dikonfirmasi secara statis lewat kode: satu `useState` di baris 96 `page.tsx`, di-pass ke kedua komponen dengan nama prop identik, mekanismenya **tidak diubah** oleh ekstraksi ini (cuma JSX consumer yang pindah file) — risiko regresi rendah, tapi rekomendasi eksplisit ke user: **verifikasi ulang klik-dari-TopBar-buka-popover-Sidebar (dan sebaliknya) di Vercel Preview dengan login normal sebelum merge**, sama seperti checklist yang sudah pernah lolos di langkah 13.

**Di luar scope (sengaja tidak disentuh):** restyle warna (lihat pembuka bagian ini). Duplikasi account menu Sidebar/TopBar. Tab nav Chat/Docs/Tasks/Sheets/Canvas dari Stitch (fitur belum ada). Tidak ada dead button/bug pre-existing baru ditemukan di region ini (beda dari langkah 13 yang menemukan tombol "Alihkan sidebar" tanpa handler).
