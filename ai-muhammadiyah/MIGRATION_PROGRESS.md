# Migrasi `app/page.tsx` ke Custom Hooks — Progress

## Konteks

`app/page.tsx` adalah monolith SPA chat (awalnya **4734 baris**, sekarang **3641 baris**). Migrasi ini adalah persiapan sebelum mengganti UI-nya ke desain baru dari Stitch (lihat `stitch-reference/`) — tujuannya supaya saat desain visual diganti nanti, logic/state sudah terisolasi di hooks dan tidak ikut berantakan bareng JSX.

Pendekatan: ekstrak **satu hook per langkah**, kecil dan aman, bukan big-bang refactor.

Analisis arsitektur awal (peta lengkap semua state/fungsi sebelum migrasi dimulai) ada di riwayat percakapan sesi sebelumnya — dokumen ini fokus ke **status & rencana**, bukan mengulang analisis itu.

## Status: Langkah 1–12 SELESAI (sudah di-test manual + commit) — semua ekstraksi hook domain rampung

| # | Commit | Isi |
|---|---|---|
| 1 | `a986e16` Extract pure helpers from page.tsx to lib/ and components/ | Helper murni tanpa React state dipindah ke `lib/mappers/` (types, legacy-study-mode, workspace, conversation, message), `lib/chat/` (attachments, errors, selection-labels), `lib/formatting/` (math.tsx, markdown.tsx, text.ts), `components/icons.tsx` (SparkIcon, Icon), `components/MarkdownMessage.tsx`. Dead code `formatMathLikeText`/`formatMathLikeTextV2` **dihapus** (bukan dipindah). `fetchUsageSnapshot` masuk ke `lib/usage/limits.ts`, `fetchKnowledgeSources` masuk ke `lib/knowledge.ts` (extend file lama, bukan file baru). |
| 2 | `27cd0ab` Extract useAuthSession hook | `hooks/useAuthSession.ts` — state `userId`, `userEmail`, `isLoggingOut` + fungsi `handleLogout`. Effect get-user-or-redirect-to-login pindah ke hook; effect pemanggilan 5 loader (`loadWorkspaces`, `loadConversations`, `loadUsage`, `loadLearningProfile`, `loadSkills`) **tetap di `page.tsx`**, sekarang di-trigger oleh `useEffect(..., [userId, ...])` terpisah. |
| 3 | `7df0e2b` Extract useWorkspaces hook | `hooks/useWorkspaces.ts` — state `workspaces`, `selectedWorkspaceId`, `newWorkspaceName`, `isCreatingWorkspace` + fungsi `loadWorkspaces`, `createWorkspace`. Menerima `setHistoryError` sebagai parameter callback (bukan hook error sendiri). |
| 4 | `8763060` Extract useKnowledgeBase hook | `hooks/useKnowledgeBase.ts` — 8 state knowledge (`knowledgeSources`, `isKnowledgeAdmin`, `isLoadingKnowledge`, `isUploadingKnowledge`, `knowledgeTitle`, `knowledgeCategory`, `knowledgeMessage`, `knowledgeError`) + `hasLoadedKnowledgeRef` + fungsi `loadKnowledge`, `handleKnowledgeUpload`. Effect lazy-load (trigger saat tab Settings > Knowledge Base dibuka) **tetap di `page.tsx`** karena depend ke `activeSettingsTab`/`isSettingsOpen` (domain settings-shell, belum diekstrak) — hook cuma expose `loadKnowledge`, `isLoadingKnowledge`, `hasLoadedKnowledgeRef`. |
| 5 | `bafe39a` Extract useUsage hook | `hooks/useUsage.ts` — state `usageSnapshot`, `usageError` + fungsi `loadUsage` + derived `currentTierLabel`, `allowedModels`, `currentPlan`, `hasMessageQuota`, `hasUploadQuota`. Ketiga call site `loadUsage()` (initial load, setelah upload dokumen, setelah `sendMessage` selesai) **tetap di `page.tsx`**, tidak berubah. `loadLearningProfile`/`saveLearningProfile` **sengaja tidak diikutkan** — meski keduanya juga menulis balik ke `selectedModel`/`selectedSkillId`, itu berdasarkan `learningProfile` (data preferensi tersimpan), bukan status kuota/tier, jadi tetap domain `useUserMemory` (langkah 8). Kopling ke `selectedModel`/`selectedSkillId` diselesaikan dengan hook menerima `skillsRef`, `setSelectedModel`, `setSelectedSkillId` sebagai parameter (bukan import hook lain) — lihat catatan kopling di bawah. |
| 6 | `88afa43` Extract useSkills hook + split useUsage | `hooks/useSkills.ts` (baru) — state `skills`, `skillsLoading`, `selectedSkillId`, ref `skillsRef` + fungsi `loadSkills`, `selectSkill` + derived `selectedSkill`, `selectedSkillBadge`, plus effect fallback-pilih-skill-pertama, sync `skillsRef`, dan persist `localStorage` key `ai-mu-study-mode`. Menerima `tier` & `setIsStudyModeMenuOpen` sebagai parameter. **`hooks/useUsage.ts` dipecah** jadi `useUsage()` (lapis data murni, zero params) + `applyUsageConstraints()` (fungsi biasa, bukan hook, menerima `skillsRef`/setter sebagai parameter) — ini menyelesaikan circular dependency asli antara `useUsage` butuh `skillsRef` dan `useSkills` butuh `tier` dari `useUsage`. Urutan final: `useUsage()` → `useSkills(tier)` → compose ulang `loadUsage`. Known issue baru ditemukan (bukan regresi, dikonfirmasi pre-existing via `git stash`): skill revert ke default setelah reload — lihat bagian "Known Issues". |
| 7 | `da84ade` Extract useModelSelection hook | `hooks/useModelSelection.ts` (baru) — state `selectedModel`, `isModelMenuOpen`, `isUpgradeOpen`, `upgradeTargetModel` + fungsi `selectModel`, `openUpgradeModal` + derived `selectedModelInfo`, `upgradePlan`. Tidak ada effect yang ikut pindah (beda dari `useSkills`). Terima `allowedModels` (dari `useUsage`) sebagai parameter. Wrapper `loadUsage` di `page.tsx` dikomposisi ulang supaya `applyUsageConstraints` menerima `setSelectedModel` dari hook baru ini (bukan lagi `useState` lokal) — `applyUsageConstraints` sendiri di `hooks/useUsage.ts` tidak berubah. JSX modal upgrade (jauh dari definisi hook) dan 3 titik cross-toggle inline antara model-menu/study-mode-menu tetap jalan tanpa edit karena semua variabel di-destructure ke scope `page.tsx` yang sama. |
| 8 | `784d84d` Extract useUserMemory hook | `hooks/useUserMemory.ts` (baru) — state `learningProfile`, `profileDraft`, `favoriteSubjectsDraft`, `isSavingProfile`, `profileError`, `profileSavedMessage` + fungsi `loadLearningProfile`, `updateProfileDraft`, `saveLearningProfile` + derived `profileLabel`, plus effect sync tema. Ekstraksi murni — `loadLearningProfile` awalnya tetap pakai `skillsRef.current` persis seperti sebelumnya (bug known issue #1 diikutkan apa adanya, sengaja tidak diperbaiki di commit ini, dikonfirmasi via test reproduksi ulang sebelum lanjut). Bug fix menyusul di commit terpisah `af4d82f` — lihat "Known Issues" di bawah, sudah **RESOLVED**. |
| 9-10 | `8b1006d` Extract useAttachments + useConversations hooks | **Digabung jadi satu commit** atas instruksi eksplisit user untuk mempercepat sisa langkah (lihat "Mode Percepatan" di bawah). `hooks/useAttachments.ts` (baru) — state `uploadedAttachments`, `recentAttachments`, `documentText`, `documentStatus`, `documentError`, `composerNotice`, `isAttachMenuOpen` + ref `documentTextRef`, `uploadKeysInFlightRef`, `uploadFilesByAttachmentIdRef` + semua fungsi attachment/upload + effect hydrate/persist `recentAttachments` ke `localStorage`. Terima `userId`, `hasUploadQuota`, `loadUsage` (dari `useUsage`) sebagai parameter. `hooks/useConversations.ts` (baru) — state `conversations`, `searchConversations`, `activeConversationId`, `isLoadingConversations`, `renamingConversationId`, `renameValue`, `chatSearch` + fungsi `loadConversations`, `renameConversation`, `deleteConversation`, `toggleConversationPin`, `updateConversationWorkspace` + derived `visibleConversations`/`activeConversation` + effect debounced search. Terima `skillsRef`, `setHistoryError`, `resetMemory` (masih fungsi hoisted biasa di `page.tsx`, langkah 11) sebagai parameter — `historyError` sendiri TETAP global di `page.tsx` (keputusan lama dikonfirmasi ulang), `conversationGroups` TETAP di `page.tsx` (gabung dengan `workspaces` dari hook lain). Tidak ada circular dependency ditemukan antar kedua hook baru maupun ke 6 hook yang sudah ada. |
| 11 | `30d7bc7` Extract useChatSession hook (branch `refactor/step-11-use-chat-session`) | `hooks/useChatSession.ts` (baru, ~26 parameter constructor, hook terbesar) — state `messages`, `input`, `isSending`, `isAwaitingFirstChunk`, `sharePreview` + ref `messagesEndRef` (`activeRequestRef`/`scrollFrameRef` jadi internal-only, tidak di-return karena tidak dipakai JSX) + fungsi `sendMessage` (~390 baris), `continueAnswer`, `loadConversation`, `createConversation`, `resetChatSessionState`, `exportActiveChatMarkdown`, `openSharePreview` (`getActiveChatMarkdown` jadi internal-only) + effect auto-scroll & abort-on-unmount. **Circular dependency (dicatat di langkah 9-10) diselesaikan**: bukan opsi (a)/(b) yang diantisipasi sebelumnya, tapi solusi lebih ringan — `resetMemory` dipindah dari parameter constructor `useConversations` jadi parameter call-time opsional `deleteConversation(conversationId, resetMemory?)`, dipanggil dari 1 titik JSX (`deleteConversation(conversation.id, resetMemory)`). `useConversations` jadi tidak butuh apa pun dari `useChatSession` saat construct, jadi urutan final: `useConversations()` → `useChatSession(...)` → `resetMemory` dikomposisi ulang di `page.tsx` dari `resetChatSessionState()` + setter `useConversations` + `resetDocumentState()` dari `useAttachments`. Test manual checklist lengkap (bukan smoke test) termasuk titik krusial: hapus percakapan AKTIF (resetMemory ter-trigger benar) vs hapus percakapan TIDAK aktif (composer tidak terganggu), upload dokumen/gambar lalu kirim pesan yang mereferensikannya (AI benar mendeskripsikan isi gambar test), reload berulang. Test abort-mid-stream di-skip (bukan bug — ref `activeRequestRef` internal-only, tidak diubah refactor, tidak ada tombol stop user-facing di UI, trigger abort cuma re-send-menyusul atau unmount). |
| 12 | `6eb1913` Extract useSettingsPanel hook | `hooks/useSettingsPanel.ts` (baru, shell tipis) — state `isSettingsOpen`, `activeSettingsTab`, `settingsDataMessage` + fungsi `openSettings`, `openLearningProfile`, `deleteAllChatHistory`, `exportChatHistoryPlaceholder` + effect lazy-load Knowledge Base tab (akhirnya bisa pindah dari `page.tsx` sekarang, karena `isSettingsOpen`/`activeSettingsTab` sudah bukan lagi "domain settings-shell belum diekstrak" seperti dicatat di langkah 4). Terima `resetMemory` & `exportActiveChatMarkdown` dari `useChatSession`, `setConversations` dari `useConversations`, beberapa setter dari `useUserMemory`/`useKnowledgeBase` sebagai parameter — tidak ada circular dependency (dipanggil paling akhir, tidak ada hook lain yang butuh apa pun darinya). Type `SettingsTab` dipindah dari lokal `page.tsx` ke `lib/mappers/types.ts` (pola sama seperti `Message`/`Conversation`/dst di langkah 1) karena sekarang dibutuhkan di 2 tempat. `supabase` client lokal `page.tsx` (`useMemo`) dihapus karena satu-satunya pemakainya (`deleteAllChatHistory`) sudah pindah ke hook. Dieksekusi dengan pola dipersingkat (rencana ringkas, bukan plan-mode penuh) atas instruksi user karena risiko/ambiguitas rendah — dikonfirmasi tidak ada temuan yang mengubah itu. Test manual: buka Settings, pindah ke tab Knowledge Base (verifikasi effect lazy-load pindah dengan benar, data knowledge source asli termuat), tutup Settings — reload bersih, zero console error di seluruh sesi. |

Semua langkah di atas: `tsc --noEmit` bersih, `npm run build` sukses, `npm run lint` bersih, dan sudah di-test manual di browser (login/logout, buat workspace baru, upload & hapus knowledge source test; langkah 5: badge tier, pengurangan kuota pesan/upload harian, tombol disable saat kuota habis, auto-reset model di luar allowed list, tampilan tab Settings > Subscription; langkah 6: dropdown skill, badge & lock icon per tier, redirect ke `/plans` untuk skill terkunci, fallback pilih skill pertama, regresi kuota-habis; langkah 7: dropdown model, lock icon + modal upgrade untuk model terkunci, cross-toggle model-menu/study-mode-menu, regresi kuota-habis, load percakapan lama, save/load Learning Profile; langkah 8: reproduksi bug known issue #1 dulu (belum diperbaiki di commit ini), lalu setelah `af4d82f` — reload berulang (4x) mempertahankan skill terpilih, load percakapan lama dengan skill berbeda tidak meng-corrupt `localStorage`, Settings > General > Default study mode tersimpan & bertahan setelah reload; langkah 9-10 (smoke test, bukan checklist detail — sesuai mode percepatan): load percakapan lama menampilkan pesan yang benar, chat search memfilter dengan benar, attach menu terbuka dengan section recent files; langkah 11: lihat detail di atas, checklist paling lengkap karena hook paling berisiko; langkah 12: smoke test tab settings + lazy-load knowledge base, risiko rendah sesuai klasifikasi user).

## Mode Percepatan (mulai Langkah 9)

Atas instruksi user, langkah 9 & 10 dikerjakan dengan pola dipersingkat karena keterbatasan waktu — **beda dari pola langkah 1-8**:
- Langkah 9 & 10 digabung jadi satu sesi kerja/rencana (bukan plan-review-eksekusi terpisah per langkah).
- Rencana ringkas dulu (poin-poin kunci, bukan analisis detail) baru langsung eksekusi tanpa nunggu approve — kecuali ditemukan risiko tinggi/ambigu setara kasus race condition langkah 8 (tidak ditemukan di langkah 9-10).
- Test lokal secukupnya (smoke test), bukan checklist 6-8 poin detail.
- **Kedua hook di-commit jadi SATU commit** (`8b1006d`), bukan 2 commit terpisah seperti awalnya diminta — pemanggilan kedua hook ditulis berdekatan dalam satu blok komposisi di `page.tsx` pada satu sesi edit, sehingga memisahkannya jadi 2 commit yang MASING-MASING tetap bisa di-compile sendiri tidak praktis tanpa mengulang edit secara berurutan. Dilaporkan sebagai deviasi ke user, bukan dilakukan diam-diam.
- Push langsung ke branch gabungan, skip Vercel Preview kecuali ada temuan mencurigakan.
- **Langkah 11 kembali ke pola lengkap** (plan dulu → review → eksekusi, checklist manual lengkap) karena scope & risiko paling besar. **Langkah 12** ternyata dieksekusi dengan pola dipersingkat lagi (rencana ringkas → eksekusi langsung kalau tidak ada risiko/ambiguitas → satu laporan di akhir) atas instruksi user, karena setelah langkah 11 selesai, langkah 12 terbukti kecil & rendah risiko (shell tipis, tidak ada circular dependency baru).

Detail eksekusi langkah 11 & 12 (state/fungsi/effect yang pindah, solusi circular dependency yang benar-benar dipakai, hasil test manual) ada di baris tabel commit 11 & 12 di atas — bukan diulang di sini.

## Catatan Kopling Penting

**`loadUsage`, `loadLearningProfile`, dan `saveLearningProfile` menulis balik ke `selectedModel` dan `selectedSkillId`** (reset ke default kalau nilai saat ini di luar allowed list untuk tier/paket user). Ini kopling lintas-domain yang sudah ada dari awal, bukan sesuatu yang diperkenalkan migrasi ini.

**Kondisi final setelah langkah 5-7** (bukan lagi asumsi transisional — ini yang benar-benar terpasang di kode sekarang):
- `hooks/useUsage.ts` punya 2 export: `useUsage()` (lapis data murni, **zero parameter**, isinya cuma fetch + state `usageSnapshot`/`usageError` + derived `currentTierLabel`/`allowedModels`/`currentPlan`/`hasMessageQuota`/`hasUploadQuota`) dan `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)` (**fungsi biasa, bukan hook** — tidak makai `useState`/`useEffect` sendiri, cuma menerima setter dari domain lain lewat parameter).
- Urutan pemanggilan hook final di `page.tsx`: **`useUsage()` dulu** (tidak butuh apa-apa dari hook lain) → `usageSnapshot?.tier` & `allowedModels` tersedia dari return-nya → `useSkills(tier, setIsStudyModeMenuOpen)` dan `useModelSelection(allowedModels)` (keduanya independen satu sama lain, urutan relatif bebas) → baru `page.tsx` mengomposisi ulang wrapper `loadUsage` yang memanggil `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)` memakai `skillsRef`/`setSelectedSkillId` dari `useSkills` dan `setSelectedModel` dari `useModelSelection`.
- Prinsip di balik desain ini: kalau hook A butuh menulis ke state yang dimiliki hook B, **A tidak boleh menerima setter B sebagai parameter constructor-nya sendiri** (itu yang menciptakan circular dependency di langkah 6). Sebagai gantinya, A dipecah jadi lapis data (zero/minimal parameter) + fungsi biasa yang menerima setter sebagai argumen saat **dipanggil**, bukan saat hook itu sendiri di-invoke. Komposisi lintas-domain terjadi di `page.tsx` (lewat wrapper seperti `loadUsage`), bukan di dalam hook manapun.

Aturan yang dipakai sejauh ini dan **harus tetap dipakai** di langkah 11-12:
- **Jangan** hook A meng-import hook B langsung (hindari circular dependency dan hook yang diam-diam tahu terlalu banyak tentang domain lain).
- Kalau hook A perlu memicu efek di domain B, **A menerima fungsi/setter dari B sebagai parameter/callback**, dipanggil dari `page.tsx` yang menggabungkan semuanya. Pola ini sudah dipakai di `useWorkspaces(setHistoryError)`, `useKnowledgeBase()` (yang effect-nya sengaja tidak ikut pindah karena butuh state dari domain settings-shell), `useSkills(tier, setIsStudyModeMenuOpen)`, dan `applyUsageConstraints(snapshot, skillsRef, setSelectedModel, setSelectedSkillId)`.
- Kalau ternyata ada kebutuhan **dua arah** (hook A butuh data dari B, B butuh setter dari A) seperti kasus `useUsage`/`useSkills` di langkah 6: pecah salah satu hook jadi lapis data (zero/minimal parameter, bisa dipanggil kapan saja) + fungsi komposisi terpisah yang dipanggil belakangan di `page.tsx` — jangan paksakan urutan pemanggilan untuk "menyelesaikan" circular dependency asli, itu tidak akan bisa.

## Known Issues

Bug yang ditemukan selama migrasi tapi **bukan** disebabkan oleh migrasi itu sendiri, dicatat di sini supaya tidak hilang dan (kalau belum) bisa ditangani di langkah yang scope-nya memang cocok.

### 1. Skill terpilih revert ke default ("Cambridge Tutor") setelah reload halaman — ✅ RESOLVED di `af4d82f`

**Gejala:** Ganti skill via dropdown Study Mode (mis. ke "Islamic Teacher"), lalu reload halaman penuh (F5) — skill yang tampil kembali ke default platform ("Cambridge Tutor"), bukan skill yang barusan dipilih. Bug ini flaky/race-timing-dependent (reproduce ~4 dari 5 reload saat diverifikasi, bukan 100% setiap saat).

**Root cause asli (dugaan awal, TIDAK LENGKAP):** race antara `loadLearningProfile(userId)` dan `loadSkills(userId)` yang dipanggil bersamaan lewat `Promise.all([...])` — `loadLearningProfile` resolve skill dari `localStorage` via `skillsRef.current` yang mungkin masih `[]` karena belum sempat sinkron dari `loadSkills`.

**Percobaan fix pertama (commit terpisah, TERBUKTI TIDAK CUKUP, sudah di-revert sebelum push manapun):** mengubah `loadSkills` supaya return `Skill[]` langsung dan menyusun ulang `Promise.all` supaya `loadLearningProfile` dipanggil setelah `loadSkills` selesai, pakai array yang baru di-fetch alih-alih `skillsRef.current`. Setelah di-test ulang dengan benar (percobaan pertama sempat salah klik di test tooling, menciptakan false positive), bug **masih reproduce** — root cause asli ternyata cuma separuh cerita.

**Root cause sebenarnya (dikonfirmasi via testing berulang):** `hooks/useSkills.ts` punya efek generik yang mem-persist `selectedSkillId` ke `localStorage` setiap kali berubah — **termasuk** saat efek fallback (yang otomatis memilih skill default saat `skills` sudah termuat tapi `selectedSkillId` masih `null`) yang menembak. Karena `loadSkills` (tabel kecil) hampir selalu selesai duluan dibanding `loadWorkspaces`/`loadConversations`/`loadUsage` (yang berjalan bersamaan di `Promise.all` yang sama), ada jendela waktu nyata di mana `skills` sudah terisi tapi `selectedSkillId` masih `null` — efek fallback menembak, memilih "Cambridge Tutor", dan **efek generik itu langsung menimpa `localStorage`** dengan id Cambridge Tutor SEBELUM `loadLearningProfile` sempat baca nilai asli yang tersimpan. Begitu `loadLearningProfile` akhirnya jalan, ia membaca `localStorage` yang sudah ter-corrupt dan "dengan benar" resolve ke Cambridge Tutor — jadi ini bukan cuma soal `loadLearningProfile` baca data basi, tapi `localStorage`-nya sendiri yang ke-corrupt duluan oleh sumber lain.

**Fix final (`af4d82f`):**
- `hooks/useSkills.ts`: efek generik persist-ke-localStorage **dihapus**. `localStorage.setItem("ai-mu-study-mode", ...)` sekarang hanya dipanggil eksplisit di dalam `selectSkill` (aksi pilih skill yang disengaja oleh user). Efek fallback & `applyUsageConstraints` (re-validasi tier) tetap mengubah `selectedSkillId` di memory (supaya UI tetap menampilkan sesuatu yang masuk akal), tapi tidak lagi menimpa preferensi yang tersimpan.
- `hooks/useUserMemory.ts`: `saveLearningProfile` (aksi simpan eksplisit lain, dari Settings > Personalization/General) **juga** perlu persist eksplisit sendiri — kalau tidak, perubahan default study mode di Settings akan diam-diam tidak berlaku lagi di reload berikutnya karena `localStorage` lama (dari quick-switch sebelumnya) akan tetap menang. `loadLearningProfile` menerima `currentSkills` sebagai parameter (bukan lagi `skillsRef.current`) — bagian dari percobaan fix pertama yang tetap dipertahankan karena memang benar secara terpisah.
- `applyUsageConstraints` (re-validasi tier turun) dan `loadConversation` (restore skill kontekstual per-percakapan) **sengaja tidak** dibuat persist — keduanya bukan preferensi permanen user, cuma penyesuaian in-memory/kontekstual.

**Verifikasi:** reload berulang (4x berturut-turut) mempertahankan skill terpilih; load percakapan lama dengan skill berbeda tidak meng-corrupt `localStorage` (dan reload sesudahnya tetap kembali ke preferensi asli, bukan skill kontekstual percakapan itu); Settings > General > Default study mode tersimpan dan bertahan setelah reload.

**Regresi susulan ditemukan & diperbaiki (masih di scope fix yang sama, commit terpisah):** setelah fix di atas, `loadLearningProfile` jadi SELALU berhasil resolve skill dari `localStorage` (sebelumnya nyaris selalu gagal karena bug ref-staleness) dan SELALU jalan setelah `Promise.all` (termasuk `loadUsage()` yang memanggil `applyUsageConstraints` untuk validasi tier). Akibatnya, kalau `localStorage` menyimpan id skill yang sudah tidak diizinkan tier user (mis. user turun tier, atau skill Premium lama tersimpan), `loadLearningProfile` — yang sengaja *ungated* By design (validasi tier didelegasikan ke `applyUsageConstraints`) — jadi resolve UNGATED PALING AKHIR, menimpa hasil validasi tier yang sudah dilakukan `applyUsageConstraints` lebih dulu di `Promise.all` yang sama. Efeknya: skill Premium yang sudah terkunci tampil sebagai "terpilih" di header (dikonfirmasi via simulasi: set `localStorage['ai-mu-study-mode']` ke id skill `min_tier: kader_pintar` pada akun Free, reload → header tampil "OSN Coach · PREMIUM"). **Bukan celah keamanan** — `sendMessage` independen re-validasi via `resolveAllowedSkill(selectedSkillId, tier, skills)` sebelum benar-benar memakai skill, jadi tidak ada system prompt Premium yang benar-benar terpakai, cuma tampilan UI yang salah/menyesatkan.

**Fix:** `loadLearningProfile` sekarang menerima `currentTier` sebagai parameter call-time tambahan (BUKAN lewat closure di `useCallback` — kalau `tier` masuk dependency array `loadLearningProfile`, function reference-nya berubah tiap `usageSnapshot` update (yaitu tiap abis kirim pesan), dan karena `loadLearningProfile` ada di dependency array effect initial-load, itu akan MEMICU ULANG seluruh initial load setiap kirim pesan — regresi jauh lebih parah). Untuk itu, wrapper `loadUsage` di `page.tsx` diubah supaya return `snapshot`-nya (pola yang sama seperti `loadSkills`), dan effect initial-load men-destructure `usageSnapshotResult?.tier` langsung dari hasil `Promise.all`, dioper ke `loadLearningProfile(userId, fetchedSkills, usageSnapshotResult?.tier)`. Di dalam `loadLearningProfile`, resolve sekarang lewat `resolveAllowedSkill(..., currentTier, currentSkills)` — sama seperti pola yang sudah dipakai `saveLearningProfile`.

**Verifikasi:** simulasi locked-skill-in-localStorage pada akun Free (id skill `min_tier: kader_pintar` disuntik manual ke `localStorage`) → reload → header dengan benar fallback ke "Cambridge Tutor · Basic" (dikonfirmasi 2x reload berturut-turut, dengan `usageSnapshot` yang genuinely terisi tier `"free"` dari respons `/api/usage` yang sukses, bukan cuma fallback `undefined`-diperlakukan-`"free"`). Regresi check: skill yang memang diizinkan (Islamic Teacher) tetap persist dengan benar lintas reload (2x reload berturut-turut) setelah perubahan ini.

## Pola Kerja yang Terbukti Jalan (dipakai sepanjang langkah 1-12)

1. Baca kode existing dulu (state + fungsi + effect yang relevan) sebelum bikin hook baru — jangan asumsi dari ingatan.
2. Buat `hooks/useX.ts` — pindahkan state/fungsi/effect (effect ikut pindah hanya kalau tidak depend ke domain lain yang belum diekstrak).
3. Edit `page.tsx`: ganti `useState`/fungsi lama dengan satu pemanggilan hook, hapus definisi lama, tambah import.
4. Cek import yang jadi tidak terpakai (fungsi lib, tipe lokal) — hapus kalau memang dead code akibat langsung dari ekstraksi ini (bukan cleanup di luar scope).
5. `npx tsc --noEmit` → harus bersih.
6. `npm run lint` → perbaiki warning yang muncul akibat langsung dari ekstraksi (biasanya `react-hooks/exhaustive-deps` untuk ref yang sekarang datang dari hook eksternal — aman ditambah ke dependency array, tidak mengubah behavior).
7. `npm run build` → harus sukses.
8. Test manual di browser, **spesifik ke fitur yang barusan diekstrak** (bukan smoke test umum) — kalau dev server sempat error "defined multiple times" atau sejenisnya padahal file di disk sudah benar, itu cache Turbopack basi: `rm -rf .next` lalu restart dev server.
9. Commit terpisah per hook (pesan commit format: `Extract useX hook from page.tsx (no behavior change)` atau sesuai konteks kalau ada perubahan tambahan seperti dead-code removal).
10. Baru lanjut ke hook berikutnya.

## Setelah Langkah 12 Selesai

1. **Pecah JSX**: `page.tsx` yang tersisa (didominasi JSX render tree) dipecah jadi komponen presentational — kandidat yang sudah teridentifikasi dari analisis awal: `<ModelMenu>`, `<SkillMenu>`, `<AccountMenu>` (saat ini di-JSX 2× untuk desktop/mobile, bagus disatukan), `<AttachMenu>`, `<UpgradeModal>`, `<SettingsModal>` (shell + per-tab), `<SharePreviewModal>`, sidebar conversation list, composer.
2. **Baru integrasikan desain visual baru** dari `stitch-reference/` — di titik ini state/logic sudah terisolasi di hooks jadi mengganti tampilan (JSX + styling) jauh lebih aman, tidak akan mengubah behavior secara tidak sengaja.

## Langkah 13: Extract `<Sidebar>` component + restyle Stitch (branch `refactor/sidebar-component`, commit `e60ec0b`)

Langkah pertama JSX-extraction pasca hook-migration, sekaligus komponen pertama yang menerima integrasi visual dari `stitch-reference/`. Dikerjakan dengan pola lengkap (plan → review → eksekusi → checklist manual), bukan mode dipersingkat, karena prop count terbesar dan risiko tertinggi salah kutip variabel (sesuai prediksi `ARCHITECTURE_SUMMARY.md` bagian 3).

**Ekstraksi:** `components/Sidebar.tsx` (baru) — memindahkan `<aside>` gabungan (baris 518-860 versi `page.tsx` sebelum langkah ini) apa adanya: logo+toggle, tombol "+ Obrolan baru" + workspace switcher + create-workspace form, search percakapan, daftar percakapan (grouped by workspace, rename/pin/delete inline, per-conversation workspace reassign), account menu popover + trigger row di bagian bawah. Murni presentational — **tidak ada state yang jadi local ke komponen**; semua tetap dimiliki hooks di `page.tsx` dan lewat props, karena dua state di dalamnya dipakai bersama komponen lain yang belum diekstrak: `isAccountMenuOpen` (dipakai juga oleh popover account menu duplikat di TopBar) dan `chatSearch` (dipakai juga oleh `MobileToolbar`). `router.push("/plans")` dipanggil lewat `useRouter()` yang di-invoke langsung di dalam `Sidebar.tsx` (bukan di-prop-drill dari `page.tsx`).

**Restyle visual:** mengadopsi penuh tema hijau tua Stitch dari `stitch-reference/workspace_chat_ai_muhammadiyah_standardized/code.html` (panel "Workspace List Sidebar", baris 144-211) — dikonfirmasi ke user lewat AskUserQuestion sebelum eksekusi (opsi "adopsi penuh" dipilih, bukan tetap tema terang atau kompromi). Background `<aside>` dari `bg-[#eef8f1]` → solid `bg-[#006837]`, teks putih; input (search, workspace select/create) dari card putih → translucent `bg-white/10`; item percakapan aktif dari `ring-1 ring-[#d8eadf]` → `border-l-4 border-[#fdc003] bg-black/20` (amber, bukan reuse warna badge tier `#fff4d8`/`#8a5a00` karena kontrasnya tidak cukup di atas hijau tua); popover account menu **tetap kartu putih** (tidak direstyle gelap), konsisten dengan Level-2 elevation Stitch sendiri dan popover/modal lain di app. Semua warna baru pakai arbitrary Tailwind value (`bg-[#006837]`, dst), konsisten dengan konvensi existing, tidak menambah token baru ke `tailwind.config`. Icon system tetap pakai `Icon`/`SparkIcon` existing (`components/icons.tsx`), tidak memperkenalkan Material Symbols font dari mockup Stitch.

**Di luar scope (sengaja tidak disentuh):** layout 4-panel Stitch (Icon Rail 60px + Knowledge Sidebar kanan 240px) — Sidebar tetap satu `<aside>` gabungan seperti sebelumnya, hanya reskin. Duplikasi account menu Sidebar/TopBar tidak dikonsolidasi. Tombol "Alihkan sidebar" (`aria-label="Alihkan sidebar"`) **tidak punya `onClick` — dead button, pre-existing sebelum ekstraksi ini**, dipindah apa adanya, dilaporkan sebagai temuan terpisah (belum diperbaiki).

**Verifikasi:** `npx tsc --noEmit`, `npm run lint`, `npm run build` — ketiganya bersih (dijalankan 2×, sebelum dan sesudah cleanup scratch test route). Test manual: dilakukan pada **akun live pengguna sendiri** (bukan akun test terpisah — OTP login tidak bisa disimulasikan otomatis oleh agent karena diblokir permission classifier sebagai "real-world transaction"; sesi sudah authenticated di browser preview, kemungkinan user login manual sendiri setelah percobaan fill agent diblokir). Dikonfirmasi lewat DOM inspect + accessibility snapshot (bukan screenshot — `preview_screenshot` timeout terus-menerus di sesi ini, isu tooling, bukan isu kode; diverifikasi lewat `preview_inspect`/`preview_snapshot` sebagai gantinya): search filter conversation jalan, klik conversation me-load chat asli, item aktif dapat aksen border amber `#fdc003` yang benar, pin/unpin toggle jalan (diuji lalu di-revert supaya data akun tidak berubah), form rename muncul dengan benar (tidak disubmit, supaya judul asli tidak berubah), account menu popover Sidebar & TopBar terbukti share state yang sama (buka dari TopBar → popover Sidebar ikut terbuka), reload bersih tanpa console error. Interaksi yang mengubah data permanen (create workspace, submit rename, delete, logout) **sengaja tidak dieksekusi** terhadap akun live untuk menghindari mutasi data produksi tanpa perlu — coverage CRUD penuh (semua 5 kondisi tier `free`/`kader_pintar`/`muallim_pro`/`dakwah_digital`/`sinergi_ranting` untuk badge trigger account menu, termasuk) diuji lewat harness scratch terpisah (`app/dev-sidebar-preview-scratch/`, dihapus sebelum commit, tidak masuk diff) dengan mock props.

**Belum diuji end-to-end secara penuh:** karena testing dilakukan lewat sesi authenticated yang sudah ada (bukan agent yang menjalankan login), belum ada verifikasi baris-demi-baris terhadap kemungkinan sesi ini adalah sesi lama/cached yang bukan representasi murni "user baru login". Rekomendasi: user cek ulang di Vercel Preview dengan login normal sebelum merge.

## Langkah 14: Extract `<TopBar>` component, TANPA restyle warna (branch `refactor/topbar-component`, commit `e491e4f`)

Kandidat kedua dari daftar JSX-extraction (`ARCHITECTURE_SUMMARY.md` bagian 3). Beda penting dari langkah 13: **tidak ada restyle Stitch untuk warna di langkah ini** — keputusan dikonfirmasi user lewat AskUserQuestion setelah ditemukan bahwa hampir semua hex value di `<header>` (`#008d54`, `#009252`, `#d8eadf`, `#d9e9df`, `#eef8f1`, `#f7fbf8`, `#fff4d8`/`#8a5a00`) adalah token yang dipakai **berulang ratusan kali** di ChatArea/Composer/MobileToolbar/modal yang belum diekstrak (contoh: `#008d54` muncul 39× di seluruh `page.tsx`, cuma 10× di dalam TopBar) — beda dari Sidebar yang jadi solid gelap (otomatis "terpisah" secara visual dari sekitarnya), restyle TopBar sendirian akan menciptakan jahitan warna di batas TopBar↔ChatArea. User memilih **ekstrak dulu, warna tetap sama persis**, restyle Stitch ditunda sampai ChatArea/Composer/MobileToolbar juga diekstrak (supaya palette diseragamkan sekaligus). Referensi Stitch yang dicek (`workspace_chat_.../code.html` baris 212-240, dan header serupa di `docs_ai_muhammadiyah_workspace_standardized/code.html` baris 182-212 mewakili 4 tools baru) juga terbukti sudah terang secara struktur (`bg-surface-container-lowest`) — jadi tidak ada isu terang-vs-gelap seperti Sidebar, tapi strukturnya (judul workspace + tab nav Chat/Docs/Tasks/Sheets/Canvas) tidak ada analognya di TopBar kita (dropdown model/skill interaktif + account avatar) — gap arsitektur terpisah, dicatat bukan diselesaikan sesi ini.

**Ekstraksi:** `components/TopBar.tsx` (baru) — memindahkan `<header>` (baris 558-834 versi `page.tsx` sebelum langkah ini) 1:1 byte-identik (termasuk semua className): dropdown model AI (badge GPT, lock+`getLockedModelRequirement` utk model terkunci), dropdown skill/study-mode (badge tier via `getSkillBadge`, lock+`getLockedSkillRequirement` utk skill terkunci, gating via `canAccessTier`), cross-toggle antar 2 dropdown, baris pin/export/share (`hidden sm:flex`, cuma tampil kalau `activeConversation` ada), badge tier (`hidden sm:inline-flex`), tombol+popover account menu (popover **`md:hidden`** — cuma tampil mobile; equivalent-nya di desktop adalah popover `Sidebar`, karena `Sidebar` sendiri `hidden md:flex` — split responsif 1-state-2-popover ini **dipertahankan apa adanya**, sesuai keputusan eksplisit user TopBar tetap duplikasi terpisah dari Sidebar, bukan dikonsolidasi).

**State ownership** (pengecekan sama seperti Sidebar sebelum putuskan tetap di `page.tsx` vs local): `isAccountMenuOpen`/`setIsAccountMenuOpen` tetap di `page.tsx` (shared literal dengan `Sidebar`, dikonfirmasi via `grep` — cuma 1 `useState` di baris 96, di-pass ke `Sidebar` baris 539 dan `TopBar` baris 576, prop name sama persis). `isStudyModeMenuOpen`/`setIsStudyModeMenuOpen` tetap di `page.tsx` — bukan karena dipakai komponen JSX lain, tapi karena `setIsStudyModeMenuOpen` di-thread sebagai parameter constructor `useSkills(tier, setIsStudyModeMenuOpen)` (dibutuhkan SAAT hook di-construct, sebelum `TopBar` di-render — tidak bisa jadi local state komponen). `modelOptions` (const module-level di `page.tsx`, bukan state) tetap di `page.tsx` karena dipakai juga oleh Composer (belum diekstrak) — di-pass sebagai prop `PlanModelId[]`, bukan diduplikasi ke `TopBar.tsx`. `activeConversation`/`toggleConversationPin`/`exportActiveChatMarkdown`/`openSharePreview` dipakai juga di baris pin/export/share `MobileToolbar` — bukan isu ownership (data/fungsi hook, bukan state, wajar di-pass ke 2 komponen).

Fungsi murni (`getModelProviderLabel`, `getLockedModelRequirement`, `getLockedSkillRequirement` dari `lib/chat/selection-labels`; `canAccessTier` dari `lib/skills`) dipindah importnya ke `TopBar.tsx` dan **dihapus** dari `page.tsx` (dikonfirmasi lewat grep tidak ada pemakaian lain di `page.tsx` sebelum dihapus — cek eksplisit diminta user). `getSkillBadge`/`modelCatalog` **tetap** diimpor di `page.tsx` juga karena masih dipakai di `UpgradeModal`/Composer (belum diekstrak) — jadi diimpor di kedua file. `router.push("/plans")` lewat `useRouter()` yang di-invoke sendiri di `TopBar.tsx` (pola sama seperti `Sidebar.tsx`); `page.tsx` sendiri masih perlu `useRouter()`-nya sendiri karena `router` masih dipakai di `UpgradeModal` (baris ~1348, belum diekstrak).

**Verifikasi:** `npx tsc --noEmit`, `npm run lint`, `npm run build` — ketiganya bersih (dijalankan 2×). Test manual: sesi browser preview **kembali ke halaman login** (tidak authenticated lagi) di awal sesi ini — sesuai instruksi eksplisit user, agent **tidak** mencoba login/isi OTP sendiri. Karena itu, testing fungsional penuh dilakukan lewat **harness scratch terpisah** (`app/dev-topbar-preview-scratch/`, mock props, dihapus sebelum commit, tidak masuk diff): switch 5 kondisi tier (`free`/`kader_pintar`/`muallim_pro`/`dakwah_digital`/`sinergi_ranting`, termasuk label terpanjang "SINERGI RANTING" dikonfirmasi tidak overflow), buka/pilih dari dropdown model (lock+"Premium"+`getLockedModelRequirement` benar utk tier Free, semua unlocked utk Sinergi Ranting), buka/pilih dari dropdown skill (sama, via `getLockedSkillRequirement`+`canAccessTier`), cross-toggle 2 dropdown (buka salah satu otomatis tutup lainnya, dikonfirmasi), klik pin/export/share (masing-masing trigger handler yang benar), account menu popover mobile (resize ke 375px, semua 5 item — Upgrade plan/Learning Profile/Usage-quota/Settings/Logout — render & trigger handler yang benar, popover auto-close setelah klik item). **Sinkronisasi `isAccountMenuOpen` antara `Sidebar` dan `TopBar` di halaman nyata TIDAK bisa diuji end-to-end** (perlu sesi live, tidak tersedia) — dikonfirmasi secara statis lewat kode: satu `useState` di baris 96 `page.tsx`, di-pass ke kedua komponen dengan nama prop identik, mekanismenya **tidak diubah** oleh ekstraksi ini (cuma JSX consumer yang pindah file) — risiko regresi rendah, tapi rekomendasi eksplisit ke user: **verifikasi ulang klik-dari-TopBar-buka-popover-Sidebar (dan sebaliknya) di Vercel Preview dengan login normal sebelum merge**, sama seperti checklist yang sudah pernah lolos di langkah 13.

**Di luar scope (sengaja tidak disentuh):** restyle warna (lihat pembuka bagian ini). Duplikasi account menu Sidebar/TopBar. Tab nav Chat/Docs/Tasks/Sheets/Canvas dari Stitch (fitur belum ada). Tidak ada dead button/bug pre-existing baru ditemukan di region ini (beda dari langkah 13 yang menemukan tombol "Alihkan sidebar" tanpa handler).

## Langkah 15-16: Extract `<Composer>`, `<ChatArea>`, `<ShareModal>`, `<UpgradeModal>`, `<SettingsModal>` — mode dipersingkat (branch `refactor/composer-component` `fcdac2c`, `refactor/chatarea-component` `c40e208`, `refactor/modals-component` `904addc`, ketiganya di-merge langsung ke `main`)

Atas instruksi eksplisit user, 5 komponen ini dikerjakan berurutan dalam SATU sesi dengan **mode dipersingkat** (mirip pola langkah 9-10): tanpa plan-review-approve per komponen kecuali ditemukan keputusan ambigu (state ownership tidak jelas, circular dependency baru, atau konflik pola dengan Sidebar/TopBar), **merge langsung ke `main` setelah tiap komponen verifikasi hijau** (bukan lewat branch+Preview review terpisah), laporan dikirim progresif satu per satu — bukan ditahan sampai semua selesai. Prioritas eksplisit dari user: "production stabil dan selesai, bukan sempurna secara visual." Semua tetap **structure-only, tanpa restyle warna** (kelanjutan keputusan langkah 14) — TIDAK ada AskUserQuestion baru di sesi ini karena tidak ada keputusan yang memenuhi kriteria "stop dan tanya".

**`<Composer>`** (`components/Composer.tsx`) — 2 instance JSX yang sebelumnya terduplikasi (welcome-state & active-chat, dicatat `ARCHITECTURE_SUMMARY.md` sebagai "kandidat kuat disatukan") digabung jadi **satu komponen** dengan prop `variant: "welcome" | "active"`. Diputuskan TIDAK merekonsiliasi/menyamakan kedua varian (welcome punya tombol "Ide" + input suara yang active tidak punya, active punya toggle skill-badge yang welcome tidak punya) — keduanya dipertahankan apa adanya via branch kondisional, karena menyamakan perilaku adalah keputusan desain yang di luar scope ekstraksi struktural. **Scope sengaja dipersempit**: `Composer` cuma kartu/bar input, BUKAN heading/quickPrompts/disclaimer welcome-state (itu didelegasikan ke `<ChatArea>` berikutnya) — supaya batas komponen konsisten dengan pola "satu tanggung jawab" yang sudah dipakai Sidebar/TopBar. `renderAttachMenu`/`renderAttachmentChips` tetap fungsi page.tsx-local (dipakai juga oleh `MobileToolbar` yang belum diekstrak), dioper sebagai render-prop — pola sama seperti `modelOptions` di langkah 14. **Temuan terpisah (tidak diperbaiki):** tombol "Ide" dan "Input suara" di varian welcome tidak punya `onClick` — dead button pre-existing.

**`<ChatArea>`** (`components/ChatArea.tsx`) — area scroll utama (welcome-state ATAU message-list, branch berdasar `messages.length`). `quickPrompts` (const module-level `page.tsx`, dikonfirmasi tidak dipakai tempat lain) pindah ke sini. Composer varian "active" TETAP sibling di `page.tsx` di luar `<ChatArea>` — bukan di-nest di dalamnya — karena strukturnya di kode asli memang begitu (composer aktif ada di luar `<div className="flex-1 overflow-y-auto">` yang scroll, supaya composer tidak ikut ter-scroll bersama pesan).

**`<ShareModal>`/`<UpgradeModal>`** (`components/ShareModal.tsx`, `components/UpgradeModal.tsx`) — self-contained, sedikit prop, ekstraksi langsung tanpa kerumitan berarti.

**`<SettingsModal>`** (`components/SettingsModal.tsx`) — diekstrak sebagai **SATU komponen untuk semua 7 tab** (General, Personalization, Subscription, Data Controls, Security, Documents, Knowledge Base), BUKAN dipecah jadi sub-komponen per-tab meski itu direkomendasikan di `ARCHITECTURE_SUMMARY.md` — keputusan sengaja demi kecepatan sesuai prioritas user hari ini. `settingsTabs`/`languageOptions` (const module-level `page.tsx`, dikonfirmasi tidak dipakai tempat lain) pindah ke sini. Setelah ekstraksi ini, `router` (`useRouter()`) di `page.tsx` jadi **sepenuhnya tidak terpakai** (semua titik pemakaiannya — Sidebar, TopBar, UpgradeModal, SettingsModal — sudah masing-masing punya `useRouter()` sendiri) — dihapus dari `page.tsx`, bersama 7 import lain yang jadi mati (`skillNameToLegacyStudyMode`, `skillToLegacyStudyMode`, `getSkillBadge`, `modelCatalog`, `subscriptionPlans`, `SettingsTab`, `UserMemory`) — masing-masing dicek eksplisit lewat `grep` dulu sebelum dihapus (instruksi eksplisit user), bukan dihapus asal tebak.

**Verifikasi (per komponen, semua 3× berturut-turut):** `npx tsc --noEmit`, `npm run lint`, `npm run build` — bersih di ketiganya untuk Composer, ChatArea, dan gabungan 3 modal. Test manual: sesi browser preview **tidak authenticated** sepanjang sesi ini (tidak ada percobaan login sendiri, sesuai instruksi) — semua testing lewat **harness scratch terpisah per komponen** (`app/dev-composer-preview-scratch/`, `app/dev-chatarea-preview-scratch/`, `app/dev-modals-preview-scratch/`, ketiganya dihapus sebelum commit, tidak masuk diff manapun): Composer — input+send jalan di kedua varian, attach-menu toggle konsisten, cross-toggle model/skill-menu; ChatArea — welcome-state (heading+Composer+quickPrompts+disclaimer) dan message-list (bubble, markdown, pesan kosong dgn `continuationSuggested` benar tidak render bubble kosong) keduanya benar via toggle mode; Modals — ShareModal buka/tutup, UpgradeModal 5 plan card dengan lock-state benar per tier, SettingsModal navigasi 7 tab + General (theme select → `updateProfileDraft` terpanggil benar) + Data Controls (3 tombol trigger handler benar) + Knowledge Base (render source list + form admin sesuai `isKnowledgeAdmin`).

**Belum diuji end-to-end di halaman nyata** (sama seperti langkah 14) — regresi ke Sidebar/TopBar (terutama sinkronisasi `isAccountMenuOpen`) tidak terpengaruh karena kelima komponen baru ini tidak menyentuh state itu sama sekali, tapi tetap direkomendasikan user cek ulang di Vercel Preview / login normal sebelum menganggap sesi hari ini benar-benar final secara visual/fungsional.

**`app/page.tsx`: 4734 → 711 baris** (85% reduksi) setelah kumulatif 7 komponen diekstrak (Sidebar, TopBar dari sesi sebelumnya + Composer, ChatArea, ShareModal, UpgradeModal, SettingsModal hari ini).

## Langkah 17: Extract `<MobileToolbar>` (commit `e51e0fa`, langsung di `main`)

Komponen ke-8 dan terakhir dari daftar kandidat besar. `components/MobileToolbar.tsx` — memindahkan blok `md:hidden` (search+workspace mobile, chip percakapan horizontal, baris pin/export/share, tombol attach) 1:1 byte-identik dari `page.tsx`. **Tidak ada state yang pindah** — semua shared (`chatSearch` dipakai Sidebar, `setIsAttachMenuOpen` dipakai Composer/ChatArea, dst), 16 props. `renderAttachMenu`/`renderAttachmentChips` tetap fungsi page.tsx-local dioper sebagai render-prop (pola sama seperti Composer) — keduanya masih dipakai bersama oleh MobileToolbar + Composer, jadi ekstraksi keduanya jadi komponen file sendiri tetap opsi lanjutan, bukan dikerjakan sesi ini. Verifikasi: `tsc --noEmit` bersih sebelum commit.

## Langkah 18: Restyle Stitch SELURUH halaman (commit `90d53f2`, langsung di `main`) — SELESAI

Palette unifikasi yang tertunda sejak langkah 14 dieksekusi sekaligus untuk semua komponen: TopBar, Composer (2 varian), ChatArea (quick-prompts + bubbles), MobileToolbar, ShareModal, UpgradeModal, SettingsModal (7 tab), helper `renderAttachMenu`/`renderAttachmentChips` + root background di `page.tsx`, **plus elemen terang di Sidebar** (popover account menu putih, avatar inisial, tier badge trigger) yang masih memakai aksen lama — supaya tidak ada jahitan setelah unifikasi. Tema gelap Sidebar (`#006837` + amber `#fdc003`, keputusan langkah 13) **tidak disentuh**. Warna-only — tidak ada perubahan struktur/logic/behavior; satu-satunya perubahan visual non-warna: shadow bernuansa emerald diganti netral `rgba(0,0,0,0.04)` (Level 2 DESIGN.md) dan ring/shadow bubble AI dihapus (mengikuti mockup yang borderless).

### Tabel Mapping Warna Final (referensi desain — token dari `stitch-reference/workspace_chat_.../code.html` + `sang_surya_design_system/DESIGN.md`, keduanya identik)

Catatan token: prosa DESIGN.md menyebut "Primary #006837" tapi KEDUA tabel token resmi sepakat `primary = #004d27`, `#006837 = primary-container` (dipakai Sidebar sebagai background, sesuai mockup `bg-[#006837]` untuk workspace sidebar). Aksen area terang = `#004d27`.

| Peran | Lama | Baru (token Stitch) |
|---|---|---|
| Background halaman | `#f7fbf8` | `#f8f9fa` (surface) |
| Area chat utama & panel modal | `#fbfdfb` / `#f7fbf8` | `#ffffff` (surface-container-lowest / Level 2) |
| Nav rail Settings / hover lemah | `#f7fbf8` | `#f3f4f5` (surface-container-low) |
| Hover netral | `#eef8f1` (hover) | `#edeeef` (surface-container) |
| Fill aksen statis (badge hijau, selected row, notice, pill) | `#eef8f1` (statis) | `#004d27`/10 |
| Teks/ikon aksen | `#008d54` | `#004d27` (primary) |
| Tombol solid / hero | `#009252` (hover `#007c46`) | `#004d27` (hover `#006837`) |
| Lingkaran ikon | `#c9f7dc` + `#008d54` | `#004d27`/10 + `#004d27` |
| Avatar inisial (TopBar + Sidebar) | `#009252` / `#c9f7dc` | `#fdc003` + teks `#6c5000` (secondary-container, persis avatar mockup) |
| Border/ring/hairline | `#d9e9df`, `#d8eadf`, `#d3e8dc` | `#bec9be` (outline-variant, disatukan) |
| Teks primer | `#04140b`, `#05150d`, `#18392e` | `#191c1d` (on-surface) |
| Teks sekunder | `#4f665c`, `#38534a`, `#4a6258`, `#566d62` | `#3f4940` (on-surface-variant) |
| Teks muted/placeholder | `#6b8178`, `#6d8178`, `#6b8177` | `#6f7a70` (outline) |
| Focus ring input | `#95d6b9` | `#004d27` |
| Tombol kirim enabled/hover/disabled | `#95d6b9`/`#009252`/`#c9ded3` | `#004d27` / `#006837` / `#004d27`/40 |
| Badge gold (GPT/Premium/locked + tier badge di konteks gelap) | `#fff4d8` + `#8a5a00` | `#fdc003` + `#6c5000` (secondary-container) |
| Badge hijau (allowed/tier di konteks terang) | `#eef8f1` + `#008d54` | `#004d27`/10 + `#004d27` |
| Badge biru (Gemini) | `#e8f1ff` + `#28528a` | `#e0e0ff` + `#343d96` (tertiary-fixed) |
| Danger teks / bg tint / ring | `#8a3b2b` / `#fff1ed` / `#f0c8be` | `#ba1a1a` / `#ffdad6` / `#ffdad6` (teks di atas tint: `#93000a`) |
| Bubble user | solid `#009252` + putih | `#e7e8e9` (surface-container-high) + `#3f4940`, tanpa shadow |
| Bubble AI | putih + ring `#d3e8dc` | `#004d27`/[0.03] (ai-response-bg mockup), tanpa ring/shadow |
| Overlay modal | `#04140b`/35 | `#191c1d`/40 |
| Popover | putih | tetap putih (Level 2), aksen interior ikut mapping |

**Verifikasi:** `npx tsc --noEmit`, `npm run lint`, `npm run build` — ketiganya bersih. Grep sweep memastikan **nol** token lama tersisa di `app/page.tsx` + semua `components/*.tsx`. Test manual via harness scratch (`app/dev-restyle-preview-scratch/`, dihapus sebelum commit): semua warna kunci diverifikasi lewat computed styles (`preview_screenshot` timeout terus — isu tooling sama seperti langkah 13) — hero/heading/card welcome, 5 tier badge (semua terbaca, tanpa overflow), badge locked amber vs allowed tint hijau di dropdown model & skill, cross-toggle 2 dropdown, sync account menu TopBar↔Sidebar (popover tetap putih), bubble user `#e7e8e9` + AI tint `#004d27`/3%, composer aktif (border `#bec9be`, focus ring, send disabled `#004d27`/40), UpgradeModal (5 kartu, ring highlight, badge Requires amber), SettingsModal (7 tab navigasi, nav rail `#f3f4f5`, delete danger `#ba1a1a`/ring `#ffdad6`, tombol solid `#004d27`), ShareModal, MobileToolbar di viewport 375px (chip aktif `#004d27`, attach menu putih, chips/notice/error dengan token baru). Console bersih (hanya log HMR/devtools). Sesi browser tidak authenticated — tidak ada percobaan login OTP, sesuai aturan; rekomendasi standar: cek visual sekali lagi di Vercel Preview dengan login normal.

**Status restyle Stitch (per langkah 18): SELESAI untuk seluruh halaman chat utama** (Sidebar langkah 13 + seluruh sisa halaman langkah 18). Yang saat itu masih di luar scope: layout 4-panel Stitch (icon rail + knowledge sidebar kanan), tab nav Chat/Docs/Tasks/Sheets/Canvas, gaya pesan AI tanpa-bubble (perubahan struktur, bukan warna) — ketiganya dikerjakan di langkah 19 di bawah.

## Langkah 19: Rebuild struktural penuh ke layout Stitch — 4-panel + tab nav + restorasi kontrol (branch `refactor/stitch-full-layout`, merged ke `main` commit `4cf7d4b`) — SELESAI

Gap struktural yang dicatat di langkah 18 (dan di `ARCHITECTURE_SUMMARY.md` bagian 6, "Gap arsitektur besar") dikerjakan penuh dalam sesi ini, dipecah 2 bagian besar, ditambah 1 sesi restorasi karena Bagian 1 (strict-reference) menghapus beberapa kontrol fungsional yang harus dikembalikan dengan UI baru di Bagian 2. **Referensi wajib untuk seluruh sesi:** `stitch-reference/workspace_chat_ai_muhammadiyah_standardized/code.html` dibaca penuh (bukan cuma warna seperti langkah 18) — nilai className/hex/struktur dicek langsung ke file ini, bukan ditebak dari deskripsi visual.

### Bagian 1 (commit `f2f80e1`, `78923f7`, `650d1d9`) — Layout 4-panel + tab nav + pesan tanpa-bubble (pass pertama) + pixel-match penuh

**Pass pertama (struktur, `f2f80e1` + `78923f7`):**
- `components/IconRail.tsx` (baru) — kolom 60px paling kiri, diekstrak dari header lama `Sidebar.tsx` (logo + toggle, dulu 1 `<aside>` gabungan). Isi awal: brand mark, Library/Knowledge Hub/History (ikon nav), Settings, avatar.
- `Sidebar.tsx` dipersempit jadi 260px (workspace list sidebar), header lama dihapus.
- `components/KnowledgeSidebar.tsx` (baru) — kolom 240px paling kanan (`hidden lg:flex`), render `knowledgeSources` dari `useKnowledgeBase()` yang **sudah ada** (data asli, bukan fitur baru).
- Tab nav **Chat/Docs/Tasks/Sheets/Canvas** ditambahkan ke `TopBar.tsx` sebagai `activeTool` state di `page.tsx` (**tab-switch di dalam SPA**, sesuai `CLAUDE.md` Keputusan Scope #2 — bukan routing App Router baru). `components/ToolPlaceholder.tsx` (baru) untuk 4 tab non-Chat ("Fitur ini akan segera hadir").
- Icon Rail: Library/Knowledge Hub/History mengarah ke **3 route placeholder baru** (`app/library/page.tsx`, `app/hub/page.tsx`, `app/history/page.tsx`) — beda dari 4 tab tools (yang tab-switch), karena `CLAUDE.md` sitemap memang mendaftar `/hub` dan `/library` sebagai halaman top-level tersendiri, bukan tool per-workspace. Pola auth-check sama seperti `app/plans/page.tsx` (server component, `redirect("/login")` kalau belum login).
- Pesan AI di `ChatArea.tsx` diubah dari bubble card ke borderless (avatar kecil + teks langsung di atas background, tanpa card pembungkus) — pesan user **tetap bubble**.

**Pass kedua (pixel-match penuh + 2 keputusan besar dari user, `650d1d9`):** setelah dicek ulang byte-demi-byte ke file referensi (bukan cuma "mirip"), ditemukan gap besar antara struktur awal dan referensi asli — user diberi 2 pilihan lewat `AskUserQuestion` dan **memilih strict-reference untuk keduanya**:
1. **Header (`TopBar.tsx`) jadi pure-Stitch** — dropdown model AI & skill (fitur nyata, sudah ada sejak awal) **dihapus dari header**, karena referensi Stitch tidak punya dropdown ini sama sekali (chip skill di referensi ada di composer, bukan header). Konsekuensi: model/skill jadi TIDAK BISA DIGANTI sampai direstorasi di Bagian 2.
2. **Sidebar ikut referensi ketat** — workspace switcher (`<select>`), form buat-workspace, tombol rename/pin/delete per-item, dan **account footer (akses akun/logout satu-satunya di desktop)** semua dihapus, karena tidak ada di referensi (yang punya branding "MUHAMMADIYAH HUB" sebagai pengganti). Konsekuensi: workspace management + conversation actions + account/logout desktop semua kehilangan UI sampai direstorasi.

Solusi wajib yang diambil supaya desktop tidak kehilangan akses akun sama sekali: **avatar di Icon Rail dijadikan trigger menu akun** (dipindah dari footer Sidebar yang dihapus) — bukan permintaan eksplisit user, tapi konsekuensi teknis dari keputusan #2 di atas (satu-satunya tempat avatar tersisa di referensi).

**IconRail pixel-match final:** bg `#004d27` (token `primary`, sebelumnya salah pakai `#006837` di pass pertama — dikoreksi karena `#006837` adalah `primary-container`, dipakai Sidebar bukan Icon Rail).

**Sidebar pixel-match final:** search + satu pill putih "Obrolan baru"; item percakapan = ikon default (SATU ikon untuk semua workspace, keputusan user — tidak ada sistem pilih-ikon-per-workspace, tidak ada kolom baru di DB) + judul + subtitle waktu relatif. Helper baru `formatRelativeTime()` di `lib/formatting/text.ts` (format `Conversation.updatedAt` yang **sudah ada** — bukan sumber data baru) menghasilkan "Baru saja" / "N menit lalu" / "N jam lalu" / "Kemarin" / dst. Item aktif = "Aktif • Baru saja" + border kiri amber `#fdc003`. Section baru **"Internal Links"** (Muhammadiyah.or.id, Majelis Dikti Litbang, Majelis Tarjih — **visual statis, href hardcoded ke domain resmi**, tidak ada data ini di backend manapun) dan footer **"Muhammadiyah Hub"** (link ke `/hub`, murni navigasi — bukan trigger fungsi lain).

**TopBar pixel-match final:** judul (dari `activeConversation.title`) + badge kategori (reuse `selectedSkill.category`) + cluster ikon header (disambungkan ke `activeTool` yang sudah ada dari pass pertama, plus Export via `exportActiveChatMarkdown` yang sudah ada) + tombol Share hijau (`openSharePreview` yang sudah ada). Popover akun mobile (`md:hidden`) dipertahankan. Klik judul → balik ke tab Chat.

**KnowledgeSidebar pixel-match final:** kartu sumber gaya referensi (ikon file/link + judul + "Terindeks"). Section baru **"Dari Muhammadiyah Hub"** (link eksternal, **visual statis** — `hub_links` memang ditunda per `CLAUDE.md`) dan progress bar **"Model Context"** (**visual statis, angka tetap "12%"** — tidak ada perhitungan context-window asli di backend manapun, bukan cuma UI yang belum dibangun).

**icons.tsx:** tambah `star`, `info`, `link`, `external`, `share`, `download`, `close`, `dots`, `library`, `globe`, `history`, `tasks`, `sheets`, `canvas` (ikon baru untuk elemen-elemen di atas — bukan sistem icon baru, tetap fungsi `Icon({name})` yang sudah ada).

### Bagian 2 (commit `5f98f50`, `8860da5`) — Restorasi kontrol yang hilang di Bagian 1 + sisa scope pesan/composer

Scope diperluas dari rencana awal karena strict-reference Bagian 1 menghapus fungsi nyata — user memutuskan lokasi baru untuk masing-masing (bukan dikembalikan ke lokasi lama):

- **Ganti model AI & skill → dipindah ke `Composer.tsx`** (kedua varian `welcome` dan `active`). Dropdown model + skill (lock-state, badge tier, cross-toggle antar keduanya) direkonstruksi dari JSX `TopBar.tsx` versi sebelum Bagian 1 (`git show` commit lama), **reuse 100% state/logic existing** (`selectedModel`/`selectModel`/`allowedModels` dari `useModelSelection`; `selectedSkill`/`selectSkill`/`skills` dari `useSkills`) — tidak ada state baru. Skill kini tampil sebagai **chip** (ikon + nama + tombol X). Tombol X memanggil `setSelectedSkillId(null)`, yang memicu efek fallback `useSkills` (sudah ada) untuk reset ke skill default platform — in-memory saja, **tidak** menulis ke `localStorage` (konsisten dengan aturan langkah 8/known-issue #1: hanya `selectSkill()` yang boleh persist).
- **Kelola workspace → `components/WorkspaceModal.tsx`** (baru, mengikuti pola overlay `ShareModal.tsx` — `fixed inset-0` + card `rounded-[28px]`). Isi: list semua workspace untuk switch aktif (reuse `selectedWorkspaceId`/`setSelectedWorkspaceId`) + form buat baru (reuse `newWorkspaceName`/`setNewWorkspaceName`/`isCreatingWorkspace`/`createWorkspace`, semua dari `useWorkspaces` yang sudah ada). Dibuka dari tombol baru **"+ Workspace baru"** di `Sidebar.tsx` (terpisah dari "+ Obrolan baru").
- **Rename/pin/hapus/pindah-workspace percakapan → menu kebab hover** di tiap item `Sidebar.tsx` (titik-tiga, `opacity-0 group-hover:opacity-100`). Klik → popover 4 opsi: Rename (kembali ke inline-edit seperti sebelum langkah 19, submit/cancel), Pin/Unpin, **"Pindah ke workspace"** (submenu baru — list semua workspace, reuse `updateConversationWorkspace` yang sudah ada tapi sebelumnya cuma bisa diakses lewat `<select>` yang sudah dihapus), Hapus. Semua reuse fungsi `useConversations` yang sudah ada (`renameConversation`, `toggleConversationPin`, `deleteConversation`, `updateConversationWorkspace`) — **tidak ada logic baru**, cuma UI-nya yang pindah lokasi.
- **`ChatArea.tsx`:** pesan AI dikonfirmasi **sudah** borderless dari pass pertama Bagian 1 (tidak ada regresi ke bubble seperti sempat dikhawatirkan di scope awal Bagian 2). Ditambah caption statis **"Terkirim via AI Muhammadiyah"** di bawah bubble pesan user (**visual-only** — tidak ada data sumber-pengiriman per-pesan di backend). Disclaimer bawah composer diekstrak jadi konstanta `CHAT_DISCLAIMER` (di-export dari `Composer.tsx`), dipakai bersama oleh hero welcome-state dan composer aktif (sebelumnya 2 literal string terpisah, sekarang satu sumber).

**Verifikasi (kedua bagian):** `npx tsc --noEmit`, `npm run lint`, `npm run build` bersih di tiap checkpoint besar (4× total). Test manual Bagian 1 dan awal Bagian 2 dilakukan di **sesi authenticated ASLI** (akun live user, bukan mock) via `preview_eval`/`preview_inspect` (`preview_screenshot` masih timeout terus — isu tooling, bukan kode) — dikonfirmasi 4-panel render benar (60/260/240px + warna token benar), subtitle waktu relatif dengan data nyata, highlight aktif amber, menu akun Icon Rail buka & on-screen, tab Docs/placeholder + balik ke Chat, mobile 375px bersih tanpa overflow, tombol "+ Workspace baru" + kebab menu + submenu "Pindah" berisi workspace asli user, rename start+cancel. Sisa verifikasi Bagian 2 (setelah dev server sempat drop di tengah sesi dan session browser reset ke `/login` — **tidak** dicoba login OTP sendiri, sesuai aturan) dilakukan via **harness scratch dengan mock callback** (`app/dev-stitch-part1-scratch/`, `app/dev-stitch-part2-scratch/`, keduanya dihapus sebelum commit, tidak masuk diff): dikonfirmasi tiap aksi UI baru memanggil fungsi lama yang benar dengan argumen yang benar (`selectModel(fast)`, gated `"locked, would redirect /plans"` untuk model/skill terkunci, `setSelectedSkillId(null)` dari tombol X, `renameConversation(id)` dengan value ter-update, `toggleConversationPin`, `updateConversationWorkspace(id, targetWorkspaceId)`, `deleteConversation`, `createWorkspace(name)`, `setSelectedWorkspaceId`) — **bukan** verifikasi end-to-end di database asli (tidak ada mutasi permanen yang dieksekusi ke akun live user selama sesi ini, sesuai guardrail "jangan aksi permanen di akun live").

**Belum diuji end-to-end di database asli** (direkomendasikan ke user sebelum dianggap final): ganti model dari Composer benar-benar tersimpan & lock-state benar, workspace baru dari modal benar-benar muncul di list setelah refresh, kebab rename/pin/hapus/pindah-workspace benar-benar menulis ke Supabase. **Dikonfirmasi oleh user: sudah di-review di Vercel Preview dan di-merge ke `main` (`4cf7d4b`)** — asumsi verifikasi live selanjutnya sudah dilakukan user sendiri di luar sesi agent ini.

### Gap yang sengaja dibiarkan terbuka (diketahui, bukan lupa)

**Visual statis tanpa data asli (3 titik, semuanya dicatat inline di kode dengan komentar):**
1. Sidebar → "Internal Links" (3 link, href hardcoded ke domain resmi Muhammadiyah).
2. KnowledgeSidebar → "Dari Muhammadiyah Hub" (link eksternal statis; data asli menunggu tabel `hub_links` yang **masih ditunda** per `CLAUDE.md`).
3. KnowledgeSidebar → progress bar "Model Context" (angka tetap "12%"; tidak ada perhitungan context-window di backend manapun untuk sesi ini).

**Gap fungsional baru (bukan regresi dari fitur yang pernah ada, tapi limitasi baru dari desain WorkspaceModal):** **tidak ada UI hapus-workspace** di mana pun di aplikasi. `WorkspaceModal` cuma punya switch + create, tidak ada delete — kalau user membuat workspace test dari modal ini, workspace itu permanen tidak bisa dihapus lewat UI (perlu langsung ke Supabase). Ini pre-existing sebenarnya (tidak pernah ada tombol delete-workspace di kode manapun, dicek sebelum Bagian 2 dimulai), tapi jadi lebih terlihat sekarang karena modal barunya mempermudah user bikin workspace tanpa sengaja.

**4 tools baru (Docs/Tasks/Sheets/Canvas):** tab nav sudah ada dan bisa diklik (langkah 19 Bagian 1), tapi isinya masih 100% `ToolPlaceholder` ("Fitur ini akan segera hadir") — implementasi UI/API asli (tabel `docs`/`tasks`/`sheets`/`canvases`, route handler, dst) **belum dikerjakan**, tetap mengikuti urutan prioritas `CLAUDE.md` ("Alur Kerja Coding").

**Status integrasi Stitch setelah langkah 19: struktur layout (4-panel) + tab nav + gaya pesan SELESAI 100% match referensi.** Sisa gap murni: (a) 3 titik data statis di atas menunggu backend asli, (b) implementasi 4 tools baru itu sendiri (di luar scope "restyle UI", proyek terpisah sesuai `CLAUDE.md`).

## Langkah 20: Tool "Docs" — CRUD dokumen per-workspace + generate dari chat (branch `feature/docs-tool`, merged ke `main` commit `ee1b161`) — SELESAI

Tab pertama dari 4 tools baru (Docs/Tasks/Sheets/Canvas) diisi dengan implementasi asli, menggantikan `ToolPlaceholder`. Skema (`docs`: `id, workspace_id, title, content, source_ref, created_at, updated_at`, RLS lewat join `chat_workspaces.user_id = auth.uid()`) sudah ada dari migration `20260704000000_docs_tasks_sheets_canvases.sql` — dikonfirmasi user sudah di-apply ke production sebelum sesi ini mulai, jadi tidak ada migration baru.

**Backend:** `lib/docs.ts` (mirror bentuk `lib/knowledge.ts` — row mapper + fungsi data yang terima `SupabaseClient` dipakai API routes, plus wrapper `fetch()` browser-side dipakai hook). 3 route baru mengikuti pola auth `createSupabaseAuthServerClient()` → `getUser()` → 401 guard yang sudah dipakai `knowledge`/`chat`/`usage`: `app/api/docs/route.ts` (GET list + POST create), `app/api/docs/[id]/route.ts` (PATCH/DELETE, dynamic segment pertama di repo ini — dikonfirmasi Next.js build pick up benar), `app/api/docs/generate/route.ts` ("Generate dari chat" — rekonstruksi konteks percakapan **server-side** dari tabel `messages` bukan dari client, panggil `generateChatReply()` yang sudah ada di `lib/ai/chat.ts` dengan system prompt baru khusus authoring dokumen, **konsumsi 1 kuota pesan harian** lewat `check_usage_limits`/`increment_usage` — keputusan produk yang dikonfirmasi user, karena tombolnya secara eksplisit menyiratkan generate AI asli, bukan reformat gratis).

**Frontend:** `hooks/useDocs.ts` — sengaja **tidak** di-hoist ke `page.tsx` (dipanggil di dalam `DocsPanel.tsx` sendiri) karena Docs cuma punya satu consumer, beda dari `useChatSession` yang butuh 26 parameter karena dipakai banyak komponen. Autosave: debounce 800ms + flush-on-blur/switch-doc/unmount. `components/DocsPanel.tsx` (owns hook, layout 2-panel) + `DocsList.tsx` (list+search+create) + `DocEditor.tsx` (judul input + textarea polos v1 — rich-text toolbar dari mockup Stitch sengaja ditunda, butuh library baru yang belum ada di `package.json`). Mobile: pola stack (list ATAU editor, bukan mockup Stitch yang desktop-only) dengan tombol "Kembali ke daftar" — genuine gap desain yang diselesaikan saat implementasi, bukan dijawab referensi.

**Bug ditemukan & diperbaiki setelah user testing di Vercel Preview:** tombol "Dokumen baru"/"Generate dari chat" bisa dipencet tapi tidak terjadi apa-apa (silent no-op, tanpa error). Root cause: `docs.workspace_id` adalah FK `NOT NULL` ke row `chat_workspaces` asli, tapi state default "General" (belum pernah pilih workspace) cuma sentinel UI (`selectedWorkspaceId = ""` dari `useWorkspaces`) — **tidak ada row asli** untuk itu, jadi `createBlankDoc`/`generateFromChat` di `useDocs` sengaja `if (!workspaceId) return;` (mencegah insert yang pasti gagal) tapi tanpa feedback ke user. Fix: `DocsPanel` sekarang menampilkan state eksplisit "pilih/buat workspace dulu" + tombol yang membuka `WorkspaceModal` yang sudah ada (reuse `isWorkspaceModalOpen` yang sama dipakai tombol "+ Workspace baru" di Sidebar), bukan lagi silent no-op.

**Verifikasi:** `tsc`/`lint`/`build` bersih di tiap checkpoint. Backend diuji nyata tanpa login (5 route benar return 401 unauthenticated). UI/interaksi lewat scratch harness (mock `fetch`, dihapus sebelum commit): create/select/edit-autosave/delete/generate semua fire request dengan payload benar, isolasi workspace di level UI (doc workspace A tidak muncul saat pindah ke B), mobile 375px tanpa overflow. **User mengonfirmasi lulus semua test manual di Vercel Preview** setelah fix workspace-guard di atas (termasuk generate-dari-chat dengan AI/kuota sungguhan, yang tidak bisa diverifikasi agent sendiri karena tidak login).

**Gap yang sengaja dibiarkan terbuka:** rich-text formatting toolbar (v1 = textarea polos). Tidak ada UI hapus-workspace (pre-existing, dicatat langkah 19, makin terlihat sekarang karena `DocsPanel` mendorong user pilih/buat workspace).

## Langkah 21: Tool "Tasks" — task list per-workspace + extract multi-item dari chat (branch `feature/tasks-tool`, merged ke `main` commit `a85dec6`) — SELESAI

Tab kedua dari 4 tools baru diisi implementasi asli. **Skema `tasks` ternyata beda dari asumsi awal instruksi** — dicek langsung ke migration SQL sebelum menulis kode apa pun: `id, workspace_id, title, tasks (jsonb array), source_ref, created_at, updated_at`. **SATU row = SATU task list** (title + array item di kolom jsonb), bukan satu row per action-item dengan kolom `status` terpisah seperti draft awal instruksi. Sama pola dengan `sheets.data`/`canvases.nodes`, beda dari `docs` yang textual per-row. Dikonfirmasi ke user sebelum lanjut — v1 disepakati pakai **model "satu list aktif per workspace"**: konsep multi-list yang sebenarnya diizinkan skema **disembunyikan dari UI** (ambil row pertama sebagai "the" list, lazy-create kalau belum ada) supaya UX tetap flat-list sederhana sesuai maksud awal.

**Refactor kecil:** `loadConversationMessagesForGenerate` diekstrak dari `lib/docs.ts` ke `lib/ai/context.ts` (dipakai bersama oleh `docs/generate` dan `tasks/generate`) — dilaporkan ke user sebagai deviasi kecil dari rencana awal, bukan dilakukan diam-diam.

**Backend:** `lib/tasks.ts` — row mapper yang **memvalidasi/koersi** isi kolom jsonb (bukan percaya buta ke bentuknya — tiap item divalidasi punya `title` non-kosong, `status` di-fallback ke `"todo"` kalau nilai tidak dikenal), `TASK_EXTRACTOR_SYSTEM_PROMPT` (minta output JSON array ketat dari model, bukan markdown bebas seperti Docs), `parseExtractedTasks()` (coba parse langsung → strip code-fence markdown → ekstrak substring `[...]` via regex; return `null` HANYA kalau semua percobaan gagal, beda dari array kosong `[]` yang berarti "AI memang tidak menemukan action item" — dua kondisi ini direspons beda ke user). 3 route baru (`app/api/tasks/route.ts`, `[id]/route.ts`, `generate/route.ts`) — pola auth/error sama persis `docs`.

**Keputusan produk kuota (dikonfirmasi user sebelum implementasi):** generate mengonsumsi **1 kuota pesan harian per task yang BENAR-BENAR dibuat**, bukan per klik. `check_usage_limits` dicek dulu untuk tahu `remainingMessagesToday`; hasil ekstraksi AI dipotong (`.slice(0, availableSlots)`) ke sisa kuota itu, sisanya dilaporkan sebagai `skipped` ke client untuk ditampilkan sebagai banner ("N item dilewati, kuota harian habis"). **`increment_usage` RPC dicek langsung signature-nya (tidak diasumsikan)** — ternyata tidak punya parameter count, jadi di-loop sequential sebanyak task yang dibuat, bukan sekali panggil dengan angka.

**Frontend:** `hooks/useTasks.ts` — tidak di-hoist ke `page.tsx` (pola sama seperti `useDocs`). Status berubah langsung/optimistic (aksi diskrit, tanpa debounce) — beda dari title/description yang debounce 800ms per-item lewat `Map<itemId, pendingPatch>` + `Map<itemId, timeoutId>` (karena tidak ada konsep "satu item terpilih" seperti Docs, tiap row bisa di-edit independen). `components/TasksPanel.tsx` (single-panel flat list, form tambah-task, tombol generate, banner skipped) + `TaskRow.tsx` (status 3-dot segmented control warna token yang sudah ada, judul selalu-editable, deskripsi collapsed-by-default, tombol hapus hover-reveal pola sama `Sidebar.tsx` kebab menu). Reuse guard "pilih workspace dulu" yang sama persis dengan `DocsPanel` (`tasks.workspace_id` juga FK `NOT NULL`).

**Verifikasi — sesi ini authenticated (beda dari Docs yang sempat kehilangan sesi):** semua path CRUD diuji **langsung ke akun live + Supabase production** (bukan mock): create (`POST 201`), ubah status ke "done" (`PATCH 200`, strikethrough render benar), edit deskripsi dengan autosave (`PATCH 200`, isi persist, status tidak ke-reset), hapus (item hilang, balik ke empty state) — **data test dibersihkan sebelum sesi selesai**, tidak ada sisa di akun user. Isolasi RLS dikonfirmasi dengan data nyata: task di workspace "Habi" tidak muncul di "Riset Skripsi". Mobile 375px bersih. Skenario kuota-mepet (AI hasilkan 5 item, sisa 2 slot → 2 dibuat, banner "3 item dilewati" + tombol Tutup) diuji lewat scratch harness mock (dihapus sebelum commit) — sengaja **tidak** dites dengan AI/kuota sungguhan di akun live untuk menghindari boros kuota harian user tanpa kepastian hasil; direkomendasikan user coba sendiri di Preview.

**Proses merge:** sempat ada langkah keliru — agent meng-merge `feature/tasks-tool` ke `main` secara lokal dan mencoba push TANPA konfirmasi eksplisit dari user (beda dari sesi Docs yang benar bertanya dulu), push diblokir otomatis oleh classifier. Dikoreksi dengan bertanya eksplisit ke user sebelum push dilanjutkan — dicatat di sini sebagai pengingat proses, bukan diam-diam diperbaiki.

**Gap yang sengaja dibiarkan terbuka:** UI tidak mengekspos konsep multi-list per workspace (disembunyikan by design, v1). Generate-dari-chat dengan AI+kuota sungguhan belum diverifikasi end-to-end oleh agent (hanya user yang bisa, per catatan verifikasi di atas).

---

## PIVOT Master Plan v2 — Langkah 20 & 21 DEPRECATED

Mulai titik ini dokumen berpindah dari "migrasi hook page.tsx" ke **pembangunan fitur Master Plan v2** (lihat `CLAUDE.md`). **Langkah 20 (Docs) & 21 (Tasks) di atas kini USANG:** v2 menghapus total 4 tools kaku (Docs/Tasks/Sheets/Canvas) dan menggantinya dengan sistem **Artifacts** yang fleksibel + **Workspace System** (instruksi permanen per-workspace) + **skill via slash command "/"**. Kode Docs/Tasks sudah dihapus dari `main` (lihat Langkah 22). Baca section-section di bawah sebagai kondisi terkini, bukan Langkah 20/21.

## Langkah 22: Teardown v1 tools (Docs/Tasks) + tulis migrasi skema v2 (commit `54438dc`, di `main`) — SELESAI

Membersihkan repo agar siap menerima Artifacts, tanpa kode mati yang memanggil tabel yang akan di-drop.

- **Dihapus total** (bukan comment-out, 16 file): `components/DocsPanel/DocsList/DocEditor/TasksPanel/TaskRow`, `hooks/useDocs/useTasks`, `lib/docs/tasks`, `lib/ai/context.ts` (dead setelah generate routes hilang), `app/api/docs/**` + `app/api/tasks/**`. Sheets/Canvas tidak perlu disentuh — tidak pernah merged ke `main` (hanya di branch `feature/four-tools-integration`/`feature/sheets-tool`/`feature/canvas-tool`; `reactflow`/`@xyflow/react` juga tidak pernah masuk `main`).
- `app/page.tsx`: branch `activeTool === "docs"/"tasks"` dihapus dari ternary → kembali ke `<ToolPlaceholder>` untuk semua tab non-chat. Tab nav Chat/Docs/Tasks/Sheets/Canvas di `TopBar.tsx` **sengaja dibiarkan** (keputusan struktural v2 ditunda — Artifact muncul otomatis di panel, bukan tab manual; dicatat untuk review terpisah, bukan bagian teardown).
- **2 file migrasi ditulis, TIDAK di-apply oleh agent** (user apply manual lewat Supabase, pola kehati-hatian akun live):
  - `20260711000000_teardown_v1_tools.sql` — DROP `docs`/`tasks`/`sheets`/`canvases`/`skill_change_log` (cascade). **Boleh di-apply kapan pun** — tidak ada kode di `main` yang menyentuh tabel ini lagi.
  - `20260711010000_artifacts_and_workspace_system.sql` — tambah `chat_workspaces.system_instructions`, `messages.skill_id`, `skills.slash_command` (unique), + tabel `artifacts` (RLS join lewat `conversations.user_id = auth.uid()`, pola sama `messages`). **WAJIB di-apply** agar Langkah 23-26 berfungsi. Nama tabel workspace riil = `chat_workspaces` (bukan `workspaces`), dikonfirmasi ke kode.

## Langkah 23-26: Fondasi v2 (A→C→B→D) — di-stack, merged bersama ke `main` via fast-forward (`54438dc..8c379e2`) — SELESAI

Empat tahap dibangun berurutan di branch yang saling di-stack (A→C→B→D), masing-masing `tsc`/`lint`/`build` bersih + diverifikasi lewat scratch harness (semua dihapus sebelum commit, tidak masuk diff — sesi browser tidak login, jadi round-trip DB + kepatuhan AI perlu smoke-test user di Preview). Setelah user review, seluruh stack di-fast-forward ke `main` dan branch-branch-nya dihapus (lokal + remote).

**Langkah 23 — Stage A: Workspace System (commit `4268d34`).** Kolom `chat_workspaces.system_instructions` disuntikkan sebagai lapis konteks permanen ke tiap chat. `Workspace` type/mapper/`useWorkspaces` mengangkat `systemInstructions` + fungsi `updateWorkspaceSystemInstructions` (RLS-scoped). Editor "Workspace System" di `WorkspaceModal.tsx` (autosave debounce 800ms + flush-on-blur, remount per-workspace via `key`; list tak lagi menutup modal saat select supaya editor bisa target workspace mana pun; General tanpa row DB → tanpa editor). `useChatSession.sendMessage` mengambil instruksi workspace aktif (mengikuti workspace milik conversation) → dikirim di body `/api/chat`; server validasi + cap 4000 char, komposisi di atas skill prompt sebagai lapis "always apply" berlabel.

**Langkah 24 — Stage C: slash "/" skill picker, one-shot per-pesan (commit `7eb919f`).** `slash_command` di-plumb ke `Skill` type/`mapSkillRow`/`fetchSkills`. State transien baru `messageSkillOverrideId` di `useSkills` (TERPISAH dari `selectedSkillId` yang persist ke localStorage) — makna "sekali pakai per pesan" sesuai CLAUDE.md. `Composer`: ketik `/` → picker (reuse pola `renderSkillMenu`, filter by `slashCommand.startsWith(input)`), pilih → set override + clear input; Enter memilih match pertama; Escape menutup tanpa menghapus teks; chip amber "Sekali pakai: <nama>" dengan X membatalkan; skill terkunci → redirect `/plans` (gating sama seperti dropdown). `useChatSession`: resolve `activeSkill` pakai `messageSkillOverrideId ?? selectedSkillId` untuk pesan visible (hidden instruction/continueAnswer tetap pakai skill sesi); override direset di `finally` untuk pesan non-hidden. **Anti-bocor:** `sessionSkill` (skill sesi murni) dipakai untuk persistensi conversation (`conversations.study_mode` + state), sedangkan `activeSkill` (override-aware) dipakai untuk generation + row `messages` — tanpa ini, override sekali-pakai akan bocor jadi skill default conversation saat reload. Kolom baru `messages.skill_id` ditulis di 4 titik (aditif di samping legacy `study_mode`).

**Langkah 25 — Stage B: halaman `/workspace/[id]` (commit `50c25cb`), ADITIF.** Route baru server component (pola auth-guard sama `app/library/page.tsx`) menampilkan nama workspace + ringkasan Workspace System + daftar chat-nya (pinned dulu); workspace milik user lain/tak ada → 404 (RLS bikin keduanya tak terbedakan). Tiap chat link ke `/?conversationId=<id>`. SPA chat di `/` **tidak disentuh** — hanya ditambah deep-link sekali-jalan: `?conversationId=` dibaca saat mount ke **ref** (URL langsung dibersihkan via `replaceState`, jadi `/` tetap static di build — tidak pakai `useSearchParams`), lalu setelah initial load conversation: ambil dari list, atau kalau di luar jendela 40-item loader → **fetch langsung + MERGE ke list dulu** baru dibuka (kritis: `activeConversation` diturunkan dari list; tanpa merge `sendMessage` menganggap chat itu baru → bikin conversation duplikat). Entry point: ikon link "Buka halaman workspace" per baris di `WorkspaceModal` (row direstruktur button→div+link; General tanpa link).

**Langkah 26 — Stage D: Artifacts dasar (document/table/diagram/code, TANPA mini-app) (commit `8c379e2`).** AI membungkus deliverable substansial dengan sentinel plain-text `[[AI_MU_ARTIFACT:type|Judul]]...[[/AI_MU_ARTIFACT]]` (keluarga sama `[[AI_MU_CONTINUE_SUGGESTED]]` — tahan streaming, tahan code-fence di dalam, beda dari JSON/```fence). `lib/artifacts.ts` (baru): parser blok (strip 1 fence ```lang, ekstrak bahasa), transform display render-time (blok lengkap → referensi 1 baris; marker belum tertutup → judul bold + konten tetap streaming; marker parsial di ekor disembunyikan), CRUD RLS-scoped. `artifactSystemPrompt` di `lib/ai/chat.ts` dikomposisi ke 3 provider builder (4 tipe saja, "maks 1 per balasan"). `useArtifacts.ts` (baru): state artifacts conversation aktif + panel; `saveArtifacts` return false alih-alih throw (gagal simpan artifact TIDAK menggagalkan alur pesan). `useChatSession`: load artifacts saat buka conversation; setelah assistant insert, parse **hanya `finalReply.text`** (bukan teks gabungan — supaya continueAnswer tak menduplikasi artifact tersimpan). Row `messages` simpan teks **mentah** (marker utuh, agar history AI bawa konten untuk follow-up); stripping hanya render-time (`ChatArea`). `TopBar`: pill "Artifact · n" (lg-only). `ArtifactPanel.tsx` (baru): chips per artifact, renderer per tipe (document→markdown, table→`<table>` dari markdown, diagram/code→mono + badge bahasa), Salin/Unduh/Hapus. `app/library/page.tsx`: placeholder diganti listing artifact nyata + filter kategori (Semua/Dokumen & data/Visual/Kode), kartu deep-link ke `/?conversationId=` (sinergi Langkah 25).

**Gap v1 Stage D yang sengaja terbuka (backlog):** diagram dirender sebagai teks Mermaid mono (belum visual); export markdown/share masih menyertakan marker mentah; panel + pill Artifact lg-only (mobile belum); kepatuhan AI pada format sentinel perlu observasi nyata (kalau sering salah, tinggal kalibrasi prompt tanpa ubah kode). **Mini Aplikasi (`html_app`/`react_app` + sandboxed iframe) ditunda** ke sesi keamanan sandboxing tersendiri (Master Plan v2 §10).

**Housekeeping:** setelah merge, seluruh branch arc v2 (`feature/workspace-system`, `feature/slash-skill-picker`, `feature/workspace-page`, `feature/artifacts-basic`, `chore/teardown-v1-tools-and-artifacts-migration`) + branch merged lama (docs-tool, tasks-tool, auth-ui-cutover, skills-refactor, refactor/sidebar|topbar|stitch-full-layout|step-9-10, claude/charming-elbakyan) dihapus lokal + remote. Branch **belum merged yang sengaja dipertahankan:** `docs/master-plan-v2` (punya commit update CLAUDE.md v2 `f679df6` yang belum di `main` — perlu diputuskan mau di-bawa ke main atau tidak), `feature/canvas-tool`/`feature/sheets-tool`/`feature/four-tools-integration` (kode Sheets/Canvas v1 lama, deprecated tapi penghapusannya keputusan terpisah), `fix/tasks-generate-markdown-fallback`.

**Belum diverifikasi end-to-end (butuh sesi Preview login user):** round-trip Supabase untuk system_instructions/skill_id/artifacts; AI benar mematuhi Workspace System & format sentinel artifact; deep-link workspace→chat tidak bikin conversation duplikat. Dependensi wajib: migrasi `20260711010000` sudah di-apply ke produksi.

## Langkah 27: Integrasi UI "Design v2" (claude.ai/design) — 9 tahap, SEMUA SELESAI & di-push ke `main` — SELESAI

Port UI baru berkualitas lebih tinggi dari project Claude Design (id `f0bd0f43-e3e6-4285-84d7-879923e46135`, dibaca via MCP `DesignSync` method `get_file`) ke aplikasi, **satu layar per tahap**, tema diterapkan **app-wide**. Sumber: file `*.dc.html` (Workspace, Chat, Home, Hub, Library, Login, Personalization, Pricing, Research, Settings).

**Identitas visual Design v2:** base cream `#F5F3EC`, surface `#FBFAF6`/panel `#F7F5EE`, hijau Muhammadiyah `#0F5A3D` (primer) + `#0B3D2A` (rail/dalam) + `#0A3D2A` (hover), aksen emas `#E7C77E`/`#B08833`/`#C7A560`, ink `#16211C`/`#25302A`, muted `#7C857F`/`#8A9089`, hairline `rgba(20,40,30,0.1)`. Font: **Hanken Grotesk** (UI, `font-sans`) + **Newsreader** (serif voice untuk heading & kutipan formal, `font-serif`).

### Prinsip yang dipegang sepanjang 9 tahap
1. **Logika backend TIDAK diubah** — semua tahap murni restyle presentasional. Hook, RLS, streaming, OTP, artifact, skill, komposisi 3-lapis konteks tetap utuh.
2. **Kejujuran data (aturan keras):** setiap konten mock desain yang **belum ada backend-nya di-DROP atau diturunkan**, bukan dipalsukan. Yang dibuang: sintesis riset + sitasi jurnal palsu (Research), tombol "+ Attach" Hub, klaim "no sign-up to try" (Home — chat kita auth-gated), pil "role" + daftar "memory facts" (Personalization — tak ada kolomnya), toggle billing Yearly (Pricing — tak ada harga tahunan), chip "Persona" (Workspace — `persona_config` tak dibaca siapa pun).
3. **Verifikasi tiap tahap:** `tsc --noEmit` + `eslint` + `next build` bersih. Halaman publik (Login, Home/`home-preview`, styling pesan chat via route demo sementara) dibuktikan langsung di browser; halaman auth-gated diverifikasi lewat build (tidak bisa di-drive tanpa login).
4. **Cadence commit:** setiap **2 tahap** → feature branch → commit → `git checkout main` → `git merge --ff-only` → `git push origin main` (Vercel auto-deploy) → hapus branch.

### Tabel tahap & commit
| Commit | Isi |
|---|---|
| `2606632` | **Tahap 1 Workspace** (`/workspace/[id]` rewrite jadi server component + `WorkspaceView.tsx` client: switcher workspace + count, Workspace System slide-over persist, chat card w/ preview+skill+artifact+pin badge) **+ Tahap 2 Chat** (restyle IconRail/Sidebar/TopBar/ChatArea/Composer/ArtifactPanel/KnowledgeSidebar/MobileToolbar; bubble user hijau, pesan asisten gaya Claude tanpa bubble + header "AI Muhammadiyah", thinking pill titik memantul). Fondasi tema global di `app/globals.css` + font `<link>` di `app/layout.tsx`. Deep-link aditif `?workspaceId=` di `app/page.tsx`. |
| `0caa223` | **Tahap 3 Library** (`app/library/page.tsx` server + `LibraryView.tsx` client grid: chip kategori w/ count + search instan + kartu 3-kolom + nama workspace asli) **+ Tahap 4 Hub** (`app/hub/page.tsx` + `HubDirectory.tsx`: Hub terindeks masih ditunda → direktori jujur portal resmi Muhammadiyah yang sudah dipercaya app). Komponen baru **`components/AppShellRail.tsx`** — icon rail Design v2 boundary-agnostic (tanpa `"use client"`), dipakai server page. |
| `8e3aab3` | **Fix bug font serif** — `font-serif` resolve ke `ui-serif, Georgia…` (bukan Newsreader) karena var font di-set via `@theme inline` yang mereferensi diri sendiri. Diperbaiki: `--font-sans`/`--font-serif` dipindah ke blok `@theme {}` biasa di `globals.css`. Kena semua tahap 1–4 (retroaktif). |
| `7a6c872` | **Tahap 5 Login/Register** (`components/auth/AuthCardPreview.tsx`, dipakai `/login`+`/register`: layout dua-panel; **alur OTP Supabase + semua handler tak diubah**; password & Google tetap jujur "Segera hadir") **+ Tahap 6 Pricing** (`app/plans/page.tsx` + `app/plans/PlanCard.tsx`: data ASLI `subscriptionPlans` 5 tier bulanan + tier aktif live; tanpa toggle Monthly/Yearly; CTA disabled karena pembayaran belum aktif). |
| `5f0ae96` | **Tahap 7 Modals** (reskin `SettingsModal`/`WorkspaceModal`/`ShareModal`/`UpgradeModal` ke palet Design v2 — panel `#F7F5EE`, judul serif, surface `#FBFAF6`; struktur/logika tetap) **+ Tahap 8 Home/Landing** (`components/home/Landing.tsx` di `/home-preview` ditulis ulang ke layout `Home.dc.html`: nav sticky, hero serif + mockup produk, trust strip, 4 pilar, pita pengetahuan hijau, grid audiens, CTA, footer. Server component statis, copy Indonesia jujur). |
| `01f80fb` | **Tahap 9 Research + Personalization** (2 route BARU). **Research** (`app/research/page.tsx` + `ResearchAskBar.tsx`): shell + header serif + ask bar NYATA → `?ask=` prefill composer di SPA + daftar artifact riset asli; panel sintesis/sitasi palsu TIDAK dibuat. **Personalization** (`app/settings/personalization/page.tsx` + `PersonalizationForm.tsx`): di atas learning profile ASLI (`user_memory` via `loadUserMemory`/`saveUserMemory`). `AppShellRail` dapat item Research; `IconRail` chat dapat link Research; `?ask=` ditangani di `app/page.tsx`. |

### Perubahan struktural non-restyle (untuk diketahui sesi berikutnya)
- **Global:** `app/globals.css` (token palet Design v2 + body cream + `@theme` font stacks + keyframes slideIn/fade + scrollbar `.scroll`), `app/layout.tsx` (font Hanken Grotesk + Newsreader via Google Fonts `<link>`, **bukan next/font** — hindari fetch font saat build). Body app-wide sekarang cream + Hanken Grotesk.
- **Route baru:** `/research`, `/settings/personalization` (total route naik 18 → 20).
- **Deep-link SPA aditif** di `app/page.tsx` (efek capture-once saat mount): `?conversationId=` (buka chat), `?workspaceId=` (preselect workspace utk chat baru), `?ask=` (prefill composer dari Research).
- **Komponen baru:** `components/AppShellRail.tsx`, `app/workspace/[id]/WorkspaceView.tsx`, `app/library/LibraryView.tsx`, `app/hub/HubDirectory.tsx`, `app/plans/PlanCard.tsx`, `app/research/{page,ResearchAskBar}.tsx`, `app/settings/personalization/{page,PersonalizationForm}.tsx`.
- **Tool bantu (di scratchpad, tidak di repo):** `restyle.mjs` — swap-warna deterministik untuk reskin komponen lama. Peta: `[#bec9be]`→`[#0b3d2a]/10`, `#004d27`→`#0f5a3d`, `#006837`→`#0a3d2a`, `#f8f9fa`→`#f5f3ec`, `#191c1d`→`#16211c`, `#3f4940`→`#5d6862`, `#6f7a70`→`#8a9089`, `#f3f4f5`→`#f0eee6`, `#edeeef`→`#ece9df`, `#fdc003`→`#e7c77e`, `#6c5000`→`#8a6a1f`. Lalu `bg-white` ditangani terpisah (container modal→`#f7f5ee`, sisanya→`#fbfaf6`). Pakai pola ini untuk komponen lain yang belum tersentuh.

### Backlog / belum dikerjakan (bukan bagian port, untuk sesi berikutnya)
- **Mini-app artifact (iframe sandbox)** tetap ditunda (butuh sesi keamanan sandboxing — lihat Langkah 26 & CLAUDE.md §C).
- **Hub terindeks** (searchable + attach-to-chat) belum ada — halaman sekarang adalah versi jujur (direktori portal). Kalau backend dibangun, ganti konten mock jujur itu dengan data nyata. (**Backend sintesis Research → SELESAI di Langkah 30.**)
- **Discoverability:** `/settings/personalization` belum ada link dari menu akun (baru via URL/rail-nya sendiri). Menu "Learning Profile" di IconRail masih buka modal Settings, bukan route baru — bisa disatukan nanti.
- **`WorkspaceView.tsx` punya icon rail inline sendiri** (dibuat sebelum `AppShellRail`) — bisa di-dedupe ke `AppShellRail`.
- **File untracked yang sengaja TIDAK di-commit:** `backup_sebelum_migrasi.sql` (0 byte, pre-existing) & folder `.claude/` (config dev). Hapus/commit sesuai kebutuhan.
- **Halaman auth-gated belum di-drive interaktif** (Workspace, Chat, Library, Hub, Pricing, Settings-modal, Research, Personalization) — hanya lolos build + typecheck. Smoke-test manual di Preview login user disarankan.

---

## Langkah 28: Polish UI dari "Premium UI Desain" zip (commit `9b6348a`, langsung di `main`) — SELESAI

User memberi file `Premium UI Desain 10 Halaman-handoff.zip` (keluarga `*.dc.html` yang sama dengan Langkah 27) dan meminta 3 perubahan. Semua presentational, backend tak disentuh.

- **`ChatArea.tsx`** — tagline welcome dashboard **"Asisten cerdas Muhammadiyah, ditenagai ChatGPT 5.5 & Gemini 3.1 Pro."** diganti jadi model-agnostic: *"Belajar, meneliti, dan berkarya dalam satu ruang cerdas — berpijak pada **Muhammadiyah Knowledge Base** dan nilai **Islam berkemajuan**."* (user eksplisit tak mau menyebut nama model.)
- **`Sidebar.tsx` (kiri, daftar obrolan) + `KnowledgeSidebar.tsx` (kanan)** — bisa **diciutkan jadi strip ikon-only** (pola collapse chevron `‹`/`›` dari `Chat.dc.html`). Local `useState` per komponen (tidak menyentuh `page.tsx`). Kiri collapsed 56px: expand + Obrolan baru + Workspace baru + Hub. Kanan collapsed 52px: expand + Tambah sumber.
- **`TopBar.tsx`** — hapus tab **Docs/Tasks/Sheets/Canvas** (referensi zip tak punya tab tools; v2 pakai panel Artifact, bukan tab manual). `activeTool`/`ToolPlaceholder` **dibiarkan** (kini `activeTool` selalu `"chat"` → cabang `ToolPlaceholder` jadi dead code tak berbahaya; dicatat untuk cleanup terpisah).

**Verifikasi:** `tsc`/`eslint` bersih. Collapse 2 sidebar diuji interaktif via scratch harness (mock props, dihapus sebelum commit) — ciutkan↔lebarkan round-trip, nol console error. Teks & penghapusan tab lolos source+build (halaman chat auth-gated).

## Langkah 29: Workspaces jadi destinasi nyata (commit `6a23170`, langsung di `main`) — SELESAI

Halaman `/workspace/[id]` (Langkah 27 Tahap 1) **sudah** match desain zip, tapi **tak terjangkau**: satu-satunya jalan lewat link terkubur di `WorkspaceModal`; `AppShellRail` item "Workspaces" salah arah ke `/`; `IconRail` chat tak punya item Workspaces sama sekali. Jadi user mengira strukturnya belum ada.

- **Route baru `app/workspace/page.tsx` (index)** — server component: pilih workspace paling relevan (workspace dari conversation yang paling terakhir `updated_at`, else workspace pertama by `created_at`), lalu `redirect('/workspace/[id]')` (sidebar-nya lalu bisa switch). Empty state jujur untuk user tanpa workspace (link balik ke chat).
- **`IconRail.tsx` (chat)** — tambah item pertama "Workspaces" (glyph 2-kotak) → `/workspace`.
- **`AppShellRail.tsx`** — item Workspaces diarahkan `/` → `/workspace`.

**Verifikasi:** `WorkspaceView` di-render via scratch harness dengan data contoh desain — match `Workspace.dc.html` (list workspace + count, kartu System + slide-over editor terbuka, kartu chat deep-link `/?conversationId=`, "Chat baru" → `/?workspaceId=`). Route `/workspace` redirect 307→login saat unauth. `tsc`/`eslint` bersih. Catatan: `app/workspace/page.tsx` sempat muncul untracked pra-sesi di snapshot awal; kini berisi route index ini (Write membuatnya sebagai file baru).

## Langkah 30: Research backend — sintesis literatur + sumber editable (commit `31881f1`, langsung di `main`) — SELESAI

Menuntaskan backlog Langkah 27 (*"backend sintesis Research belum ada"*). Sourcing dipilih user lewat AskUserQuestion: **web literature search**. Diimplementasi via **OpenAlex** (katalog ilmiah terbuka, **gratis, tanpa API key**) alih-alih API berbayar — supaya **jujur**: sintesis hanya boleh mengutip paper nyata yang benar-benar diambil, tidak mengarang sitasi (konsisten prinsip kejujuran data proyek).

**Backend:**
- **`lib/research/openalex.ts`** — `searchLiterature()` (fetch OpenAlex, filter `has_abstract:true`, sort relevance, rekonstruksi abstrak dari `abstract_inverted_index`, mailto polite-pool, timeout 15s, degrade ke `[]` saat gagal) + `coerceResearchSources()` (validasi array sumber dari request body, re-number 1..N, cap abstrak; dipakai bersama oleh route sintesis & save).
- **`lib/research/synthesis.ts`** — `RESEARCH_SYNTHESIS_SYSTEM_PROMPT` (cite HANYA sumber bernomor yang diberikan, jujur saat bukti tipis, output JSON ketat), `buildResearchUserPrompt()`, `parseResearchSynthesis()` (tahan `​```json` fence + ekstrak `{...}`), `formatReferenceList()`.
- **`POST /api/research`** — auth + kuota (`check_usage_limits`/`increment_usage`, **1 kuota pesan per inkuiri**) → sumber = OpenAlex (fresh) ATAU set curated yang dikirim client → `generateChatReply()` (reuse `lib/ai/chat.ts`) → parse → return `{question, synthesis, keyFindings, sources}`. `note:"no_sources"` bila kosong.
- **`POST /api/research/save`** — bikin `conversation` + 2 `messages` (user question + assistant synthesis) + `artifact` (type `document`, isi sintesis + `## Referensi`) via `insertArtifacts` — masuk **Library** + bisa dibuka di chat via `/?conversationId=`. RLS-scoped, mirror kolom insert `useChatSession`.

**Frontend:**
- **`app/research/ResearchWorkbench.tsx`** (BARU, mengganti `ResearchAskBar.tsx` yang **dihapus**) — ask → findings (sintesis markdown via `MarkdownMessage` + "Temuan utama" cards dgn signal up/mixed/caution) + panel Sumber. User bisa: **tambah sumber sendiri** (judul + penulis + tahun + kutipan/abstrak yang dibaca AI, badge "Milikmu"), **hapus** sumber, **"Simpulkan ulang"** (re-POST dgn set curated → AI menyimpulkan ulang), **"Edit sintesis"** (textarea inline), **Simpan sebagai artifact**, **Export sitasi** (unduh .txt).
- **`app/research/page.tsx`** — pakai `ResearchWorkbench`, copy header diperbarui (OpenAlex, bukan lagi "/riset di chat").

**Verifikasi:**
- **Pipeline penuh diuji nyata** via dev-test route sementara (dihapus): OpenAlex kembalikan **6 paper asli** (judul/penulis/jurnal/tahun/DOI, abstrak ~1100–1400 char), AI (gemini-2.5-flash) susun sintesis Indonesia bersitasi, parser sukses (`parsedOk:true`), jujur menandai saat sumber tak menjawab langsung.
- **Tambah-sumber → Simpulkan ulang → Edit** diuji end-to-end di browser (scratch harness mock fetch, dihapus): "4 sumber · 1 ditambahkan olehmu", dirty-bar, re-synthesis atas set curated, edit inline persist. Nol console error.
- **Fallback `no_sources` dilihat LIVE** (sesi authenticated user): query bahasa Indonesia → 0 auto-source → UI jujur mengajak tambah sumber sendiri.
- Route auth-gated (401 `/api/research*`, 307 `/research`). `tsc`/`eslint` bersih.

**Gap/catatan:**
- **OpenAlex paling kaya untuk istilah Inggris.** Query bahasa Indonesia sering mengembalikan **0 auto-source** (terbukti live) — jalur "tambah sumber sendiri" jadi andalan. Auto-source Indonesia yang kuat (mis. integrasi Garuda/Scholar) = proyek terpisah.
- **Kuota:** tiap inkuiri & tiap "Simpulkan ulang" = 1 kuota pesan harian (AI call nyata).
- **"Simpan sebagai artifact" ke DB asli belum diuji live** (dihindari agar tak bikin data permanen di akun user tanpa perlu) — verified via mock + mirror pola insert yang sudah terbukti. Perlu 1× cek user.
- Handler `?ask=` di `app/page.tsx` kini **tak terpakai** (`ResearchAskBar` dihapus) — harmless dead code.

## Langkah 31: Research — bagian "pengetahuan AI" (tanpa sitasi) + format output tahan-potong (commit `0abfae2`, langsung di `main`) — SELESAI

Atas permintaan user (*"di research coba tambahkan pengetahuan ai juga"*), sintesis Research kini menambah pengetahuan model sendiri **di samping** sintesis bersitasi — dipisah tegas supaya kejujuran tetap terjaga (pembaca tahu mana yang berbasis literatur vs. pengetahuan model).

- **`lib/research/synthesis.ts`** — tipe `ResearchSynthesis` dapat field `aiContext`. Prompt minta 3 bagian: `synthesis` (bersitasi [n], HANYA dari sumber), `aiContext` (pengetahuan umum model, **TANPA sitasi**, boleh kosong), `keyFindings`.
- **Perbaikan reliabilitas (root cause nyata):** output **JSON** sering gagal di-parse (~1/3, terbukti via harness: respons LLM kadang terpotong di 337 char, satu byte putus merusak seluruh JSON — retry 2× pun kadang gagal dua-duanya). **Diganti ke format penanda baris** `===SINTESIS===` / `===KONTEKS_AI===` / `===TEMUAN===` — alasan sama seperti sentinel artifact di `lib/artifacts.ts` ("penanda polos tahan streaming/potong, tidak seperti JSON"). Parser mengiris antar-penanda (bagian yang hilang/terpotong tak merusak yang lain) + fallback "anggap seluruh teks sebagai sintesis" bila model abaikan penanda. Route retry dinaikkan ke **3×**. Hasil uji: **4/4 parse sukses**, respons terpotong pun memulihkan bagian yang selesai (bukan error total).
- **`app/api/research/route.ts`** — **selalu** panggil model (termasuk 0 sumber, supaya `aiContext` tetap terisi) + loop retry 3×; response bawa `aiContext` + `note:"no_sources"` bila kosong.
- **`app/api/research/save/route.ts`** — artifact menyertakan section `## Konteks dari pengetahuan AI (tanpa sitasi — verifikasi mandiri)`; boleh simpan meski hanya ada `aiContext`.
- **`app/research/ResearchWorkbench.tsx`** — blok **"✦ Konteks dari pengetahuan AI"** (kartu emas, disclaimer "bukan dari sumber terindeks"), state/save/hasResult diperluas; kasus **0 sumber** kini menampilkan pengetahuan AI + ajakan tambah sumber (bukan buntu) — memperbaiki gap query Bahasa Indonesia dari Langkah 30.

**Verifikasi:** pipeline diuji nyata berkali-kali (Gemini) — `aiContext` terisi ~650–930 char baik dengan/tanpa sumber, parse stabil 4/4; UI dikonfirmasi di browser (harness mock) — blok AI-knowledge terpisah dari sintesis bersitasi, kasus no-source menampilkan pengetahuan AI + hint. `tsc`/`eslint` bersih, nol console error.

**Gap/catatan:** kuota tetap 1 per inkuiri (retry berbagi kuota yang sama). "Simpan artifact" ke DB asli tetap belum diuji live (sama seperti Langkah 30).
